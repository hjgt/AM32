# AM32 / STM32G431 + FD6288 移植 —— 拨乱反正分析

> 复盘目的：这份移植说明从"移植"一路演变成"重新开发"，核心争议集中在过零检测(ZCD)。
> 本文基于源码 + 移植说明 + 你最新的万用表数据，回答三件事：
> 1. 新测的 PA6 / PA15 / PB7 阻抗数据说明了什么；
> 2. 到底能不能用硬件比较器；
> 3. 哪里"跑题"了，正确的落脚点在哪。

---

## 〇、结论先行（TL;DR）

1. **你的新发现很重要，它直接推翻了第十一章的地基结论**：第十一章说"本板没有虚拟中性点"，那是因为**当时只测了 PA0/1/2/5，漏测了 PA6/PA15/PB7**。PA6 到 A/B/C 三相**严格等阻(107K)**，这是"对称耦合到三相"的确凿电气特征——**这块板极可能有物理虚拟中性点，就在 PA6 上**（PA15/PB7 是另外两套三相对称网络）。原厂闭源固件多半就是读这个硬件中性点做 ZCD 的。所以"量产板画得对程序不友好"这个怀疑是对的——不是板子不友好，是**之前没找到板子给的那个脚**。

2. **但这对当前固件"要不要改"影响很小**：软件用 `(Va+Vb+Vc)/3` 重构中性点，数学上等价于真实星点，而且**日志(12.16)已经证明闭环稳定锁定了**。硬件中性点能省的只是"多读两路 ADC"，当前瓶颈根本不在这。

3. **硬件比较器：受引脚复用硬约束，纯片内方案做不干净，文档这个结论是对的**——但理由要更新为下面第二节的精确版本。除非你愿意飞一根线，否则不建议为它折腾。

4. **真正的"跑题"在于**：在 ZCD 上投入了路线 B / 路线 C / 实验 A/B/C/D 一大串，最后 12.16 自己发现——**闭环其实早就好了，转速上不去是 `low_rpm_throttle_limit` 这个 AM32 原生保护 + 电机 KV/极对数没配对**，一个配置项的事。这就是"越改越像重新开发"的由来：病根是配置，药却下在了算法上。

---

## 一、新阻抗数据的电气解读（决定性）

### 1.1 数据对照

| 引脚 | ->A | ->B | ->C | 特征 |
|---|---|---|---|---|
| PA0 | **5.1K** | 7.1K | 7.1K | 只与相 A 直连(5.1K)，到他相是间接 → **相 A 分压抽头** |
| PA1 | 7.1K | **5.1K** | 7.1K | 相 B 分压抽头 |
| PA2 | 7.1K | 7.1K | **5.1K** | 相 C 分压抽头 |
| PA5 | 7.1K | 7.1K | 7.1K | 三相**间接相等**(1.0+1.0+5.1)，纯经 GND → **空脚 + 1K 下拉** |
| **PA6** | **107K** | **107K** | **107K** | 三相**直接相等**，非间接可凑 → **对称耦合到三相** |
| **PA15** | **12.5K** | **12.5K** | **12.5K** | 同上，另一套对称网络 |
| **PB7** | **22.5K** | **22.5K** | **22.5K** | 同上，第三套对称网络 |

### 1.2 为什么"三相等阻"≠"PA5 那种间接相等"

- PA5 的 7.1K 能被 `1.0K(自身下拉) + 1.0K(他相下拉) + 5.1K(他相上臂)` 精确凑出，是**纯经 GND 的间接路径**，所以判它为空脚——**这个判断当时是对的**。
- 但 PA6 的 **107K 没法用板上任何 5.1K/1.0K 的串并联凑出来**。要让一个脚到三相**同时都是 107K**，物理上只有一种自然解释：**存在三个相等的大电阻，从 A、B、C 分别汇聚到 PA6 这个公共节点**。

用节点法反推（相端子对地阻抗 Z≈6.1K，设汇聚电阻为 R）：

```
测 PA6->A = R(直连臂)  并联  [ (R + 2Z)/2 (经 B、C 两条对称间接臂) ]
R >> Z 时 ≈ R ∥ (R/2) = R/3
```

由 107K ≈ R/3 反推 **R ≈ 320K**，回代校验：`(320+12.2)/2 = 166K`，`320 ∥ 166 ≈ 110K ≈ 107K` ✓ 自洽。

> 也就是说：PA6 通过约 300K 量级的三只对称电阻接到三相 —— **这就是教科书式的星点电阻网络（虚拟中性点）**。PA15(12.5K)、PB7(22.5K) 同理是另外两套对称网络（阻值不同，服务的功能可能不同，比如相电压求和/滤波/检测）。

### 1.3 对第十一章结论的修正

> 第十一章原文："本板没有物理虚拟中性点，PA5 只是空脚。"
>
> **应修正为**：PA5 确实是空脚；但**真正的虚拟中性点/三相对称节点在 PA6（以及 PA15、PB7）上，第十一章漏测了这三个脚**。当时用 PA5 当基准检测不出过零，不是因为"板上没有中性点"，而是**基准脚认错了**。

**验证方法（5 分钟，示波器）**：跑原厂闭源固件，电机转动时量 PA6：
- 若是**跟随母线电压变化的稳定直流（≈Vbus/2 分压值）** → 确认 PA6 就是硬件虚拟中性点，原厂就是读它做 ZCD 的；
- 若恒 0 或跟着某一相跳 → 另作他用。
- PA15/PB7 同法排查，找出哪套是真中性点、哪套是相电压/电流检测。

---

## 二、硬件比较器到底能不能用？

### 2.1 精确约束（来自 `stm32g4xx_ll_comp.h`，已核对本工程驱动）

G431 每个比较器的输入引脚是**写死**的，本板三相 BEMF 在 PA0/PA1/PA2：

| 相 | 引脚 | 可作比较器**正**输入(INP)？ | 可作比较器**负**输入(INM)？ |
|---|---|---|---|
| A | PA0 | 是 (COMP3_INP_IO1) | 是 (COMP1_INM_IO2) |
| B | PA1 | 是 (COMP1_INP_IO1) | **否** |
| C | PA2 | **否** | 是 (COMP2_INM_IO2) |

**死结**：ZCD 要求三相轮流当"悬空相"接到比较器的**同一个极性输入**。但——
- 要三相全走**正**输入：PA2 不行；
- 要三相全走**负**输入：PA1 不行。

**无论哪种极性，PA1 与 PA2 的复用彼此打架，必有一相对不上。** 这就是"片内比较器做不了共享中性点的 6 步 ZCD"的**真正原因**（比原文档"PA2 只能当负端"更完整）。

### 2.2 那多比较器 + 硬件中性点方案呢？

即便现在知道 PA6 是硬件中性点，也救不了：

- 中性点要进比较器**负端**，必须落在 `COMPx_INM` 引脚（PA4/PA5/PA0/PA2...），**PA6 不在任何比较器输入引脚列表里**（PA6 是 DAC2_OUT1/OPAMP 区，不是 COMP IO）。所以**PA6 这个硬件中性点没法直接喂给片内比较器**。
- 三相各用独立比较器（COMP1 收 PA1、COMP3 收 PA0）仍然卡在 **PA2 无正输入**。

**结论：在不动硬件的前提下，片内硬件比较器方案不可行。文档放弃它是正确的，只是当初的理由（"没有中性点"）是错的，正确理由是"PA0/1/2 的比较器复用相互冲突 + 中性点脚 PA6 非比较器 IO"。**

### 2.3 唯一能上硬件比较器的路（需飞线，不推荐现在做）

如果哪天真想极致省 CPU、最接近原厂：
- 把**相 C 的 BEMF 从 PA2 飞线挪到 PA7**（PA7 = COMP2_INP_IO1，且 PA7 当前只作 ADC2_IN4 电流采样的候选、G431 目标未真正占用为相 C）；
- 三相分别用 COMP1(PA1)/COMP2(PA7)/COMP3(PA0) 的正输入；
- 负端统一接 DAC 输出的门限，或接一个落在 COMP_INM 引脚上的中性点。

**代价**：一根飞线 + 三比较器初始化 + EXTI 换相切换逻辑，改动不小，且当前软件方案已能跑。**性价比低，除非你就是想复刻原厂。**

---

## 三、当前软件方案评价 + 真正该做的事

### 3.1 现方案其实是"这块板布线下的合理解"

- 受 2.1 的引脚死结限制，**软件 ADC ZCD 是这块板不飞线时唯一干净可行的路**——这一步没跑题。
- 路线 C（TIM1_CH4 触发 ADC 注入组、PWM 同步采样）是**正确且专业的做法**，和主流 ESC（BLHeli_32 / AM32 官方 ADC 板）思路一致。
- `(Va+Vb+Vc)/3` 软件中性点**数学上等价于真实星点**，且不依赖分压比和母线电压读数，比读 PA6 硬件中性点更鲁棒（省了一路 ADC 校准）。**即使 PA6 是真中性点，也没有必要改回去。**

### 3.2 跑题点复盘

| 阶段 | 投入 | 事后看 |
|---|---|---|
| 路线 B（软件重构中性点） | 改 getCompOutputLevel | 方向对，但当时采样时机没同步，无效 |
| 路线 C（注入组同步采样） | 改 ADC/peripherals/comparator 三文件 | **这一步才是关键，做对了** |
| 边沿修 RISING->FALLING | 单行 | 必要修复 |
| 实验 A+B（钳位+消隐） | 两处 | **让闭环真正锁定，做对了** |
| 实验 C（双边沿翻倍采样率） | 计划中、改动大 | **12.16 已判定不需要**——转速封顶不是采样率问题 |

**真正的教训**：12.14 把"转速封顶"归因为"注入采样率量化不够"（并准备上改动很大的实验 C），而 12.16 用日志证明——**闭环早就锁定了，封顶是 `low_rpm_throttle_limit` 保护 + 电机 KV/极对数没配对**。差点为一个配置问题去大改采样架构，这就是"越改越像重开发"的根源。

### 3.3 建议的落脚顺序（按性价比）

**第一优先——配置层，零代码（这才是当前真问题）：**
1. 在 AM32 配置器里**如实填 `motor_kv` 与 `motor_poles`（你说是 14 磁极 = 7 对极）**。
2. 若电机实际 KV < 300，程序会自动关掉 `low_rpm_throttle_limit`（main.c:771-773）；否则它会按 KV 重算 `high_rpm_level`，让占空比更早放开。
3. 复测：给油门看 `commutation_interval` 是否随油门下降、转速是否跟随。**大概率转速封顶当场就解决了。**

**第二优先——若配置改不动再动源码（仍不碰闭环算法）：**
- 在 `Inc/targets/CUSTOM-FD6288-G431.h` 里编译期覆盖：提高 `high_rpm_level` 或直接令 `low_rpm_throttle_limit` 默认关闭。属配置性质，非算法改动。

**不建议现在做：**
- 实验 C（双边沿采样）——12.16 已证明非必要，先别加复杂度。
- 硬件比较器 / 飞线——现方案能跑，投入产出比低。

**可选的锦上添花（好奇心/文档正确性）：**
- 用示波器验证 PA6 是不是硬件中性点（3.1 的方法），把结论回填第十一章，纠正"本板无虚拟中性点"的错误记载。这对当前固件没有必须的改动价值，但能让这份移植说明**从错误结论中解脱出来**。

---

## 四、给移植说明文档的勘误建议

请在第十一章补一条勘误（不必删原文，保留调试轨迹）：

> **勘误(本次)**：11.2 "本板没有虚拟中性点"的结论仅基于 PA0/1/2/5 四脚，**漏测了 PA6/PA15/PB7**。后续实测 PA6->A/B/C 三相严格等阻(107K)、PA15(12.5K)、PB7(22.5K)，均为"对称耦合三相"特征，**本板极可能有物理虚拟中性点(候选 PA6)**。软件 `(Va+Vb+Vc)/3` 方案与之等价、无需改动；硬件比较器仍因 PA0/1/2 比较器复用冲突 + PA6 非 COMP IO 而不可用。转速封顶的真因见 12.16（`low_rpm_throttle_limit`），非 ZCD 问题。

---

*本分析基于：`comparator.c`、`ADC.c`、`peripherals.h`、`stm32g4xx_ll_comp.h`(比较器引脚表)、移植说明第 11/12 章，以及你提供的 PA6/PA15/PB7 阻抗实测数据。*
