# AM32 固件移植说明 — STM32G431KBU6 + FD6288

> 硬件目标：自制 ESC 板，主控 STM32G431KBU6，栅极驱动 FD6288  
> 固件基础：[AM32 开源 ESC 固件](https://github.com/AlkaMotors/AM32-MultiRotor-ESC-firmware) + [AM32 Bootloader](https://github.com/AlkaMotors/AM32_Bootloader)  
> 编译工具链：`arm-none-eabi-gcc 13.2.1`（`/usr/bin/arm-none-eabi-`）

---

## 一、硬件引脚映射

| 功能 | MCU 引脚 | 定时器/外设 | AF |
|---|---|---|---|
| 高侧 A（PWM） | PA8 | TIM1_CH1 | AF6 |
| 高侧 B（PWM） | PA9 | TIM1_CH2 | AF6 |
| 高侧 C（PWM） | PA10 | TIM1_CH3 | AF6 |
| 低侧 A（PWM） | PA11 | TIM1_CH1N | AF6 |
| 低侧 B（PWM） | PA12 | TIM1_CH2N | AF6 |
| 低侧 C（PWM） | PF0 | TIM1_CH3N | AF6 |
| DSHOT / 信号输入 | **PB5** | TIM3_CH2 | AF2 |
| 遥测串口（USART1 TX） | PB6 | USART1 | AF7 |
| BEMF A（ADC） | PA0 | ADC1_CH1 | — |
| BEMF B（ADC） | PA1 | ADC1_CH2 | — |
| BEMF C（ADC） | PA2 | ADC1_CH3 | — |
| 下拉引脚（ADC） | PA5 | ADC1_CH13 | — |
| 母线电压（ADC） | PA3 | ADC1_CH4 | — |

> **硬件限制说明**：STM32G431 上 PA1=COMP2_MINUS_IO1、PA2=COMP2_MINUS_IO1，只能作为比较器**负端**输入，**无法**用作 BEMF 正端输入。因此本移植完全放弃硬件比较器 ZCD，改用 **软件 ADC 轮询 ZCD**。
>
> **过零基准说明（见第十一章）**：万用表阻抗矩阵逆向证实本板**无物理虚拟中性点**——PA5 只带一只 1.0K 下拉、未接任何相，是"第 4 路 BEMF 式分压输入"预留但上臂未装的空脚。因此过零基准**不再读 PA5**，改为软件重构 `neutral = (Va+Vb+Vc)/3`。表中 PA5(`NEUTRAL_ADC_CHANNEL`)一行仅作历史保留，运行时不再采样。


---

## 二、主固件移植（AM32-main）

### 2.1 新增目标 `CUSTOM_FD6288_G431`

**文件：`Inc/targets.h`**

在文件末尾、`#ifndef FIRMWARE_NAME` / `#error` 保护宏**之前**，添加如下 target 块：

```c
#ifdef CUSTOM_FD6288_G431
#define FIRMWARE_NAME           "FD6288-G431"        // 最多 12 字符
#define FILE_NAME               "CUSTOM_FD6288_G431"
#define MCU_G431
#define USE_TIMER_3_CHANNEL_2

// ── ADC 软件 ZCD（USE_ADC_ZCD）──────────────────────────────
#define USE_ADC_ZCD
// BEMF A=PA0, B=PA1, C=PA2 → ADC1 CH1/2/3
#define BEMF_A_ADC_CHANNEL      LL_ADC_CHANNEL_1
#define BEMF_B_ADC_CHANNEL      LL_ADC_CHANNEL_2
#define BEMF_C_ADC_CHANNEL      LL_ADC_CHANNEL_3
// NEUTRAL_ADC_CHANNEL(PA5) 已废弃：万用表证实本板无虚拟中性点，PA5 只是空脚+1K下拉。
// 过零基准改为软件重构 (Va+Vb+Vc)/3，见第十一章。此宏保留仅为兼容编译，运行时不再读取。
#define NEUTRAL_ADC_CHANNEL     LL_ADC_CHANNEL_13   // 已废弃，空脚
// 母线电压 PA3 → ADC1 CH4
#define VOLTAGE_ADC_PIN         LL_GPIO_PIN_3
#define VOLTAGE_ADC_CHANNEL     LL_ADC_CHANNEL_4


// ── COMP 占位符（ADC ZCD 路径下仍需编译，给合法值即可）──────
#define PHASE_A_COMP            LL_COMP_INPUT_MINUS_IO2
#define PHASE_A_INPUT_PLUS      LL_COMP_INPUT_PLUS_IO2
#define PHASE_A_EXTI_LINE       LL_EXTI_LINE_22
#define PHASE_A_COMP_NUMBER     COMP2
#define PHASE_B_COMP            LL_COMP_INPUT_MINUS_IO1
#define PHASE_B_INPUT_PLUS      LL_COMP_INPUT_PLUS_IO1
#define PHASE_B_EXTI_LINE       LL_EXTI_LINE_21
#define PHASE_B_COMP_NUMBER     COMP1
#define PHASE_C_COMP            LL_COMP_INPUT_MINUS_IO2
#define PHASE_C_INPUT_PLUS      LL_COMP_INPUT_PLUS_IO2
#define PHASE_C_EXTI_LINE       LL_EXTI_LINE_22
#define PHASE_C_COMP_NUMBER     COMP2

// ── DSHOT 输入：PB5 / TIM3_CH2 ──────────────────────────────
#define INPUT_PIN               LL_GPIO_PIN_5
#define INPUT_PIN_PORT          GPIOB
#define IC_TIMER_CHANNEL        LL_TIM_CHANNEL_CH2
#define IC_TIMER_REGISTER       TIM3
#define INPUT_DMA_CHANNEL       LL_DMA_CHANNEL_1
#define IC_DMA_IRQ_NAME         DMA1_Channel1_IRQn

// ── PWM 高侧 ─────────────────────────────────────────────────
#define PHASE_A_GPIO_HIGH       LL_GPIO_PIN_8
#define PHASE_A_GPIO_PORT_HIGH  GPIOA
#define PHASE_B_GPIO_HIGH       LL_GPIO_PIN_9
#define PHASE_B_GPIO_PORT_HIGH  GPIOA
#define PHASE_C_GPIO_HIGH       LL_GPIO_PIN_10
#define PHASE_C_GPIO_PORT_HIGH  GPIOA

// ── PWM 低侧 ─────────────────────────────────────────────────
#define PHASE_A_GPIO_LOW        LL_GPIO_PIN_11
#define PHASE_A_GPIO_PORT_LOW   GPIOA
#define AF_A_LOW                LL_GPIO_AF_6
#define PHASE_B_GPIO_LOW        LL_GPIO_PIN_12
#define PHASE_B_GPIO_PORT_LOW   GPIOA
#define AF_B_LOW                LL_GPIO_AF_6
#define PHASE_C_GPIO_LOW        LL_GPIO_PIN_0
#define PHASE_C_GPIO_PORT_LOW   GPIOF
#define AF_C_LOW                LL_GPIO_AF_6

#define DEAD_TIME               70
#define VARIABLE_PWM
#define USE_DSHOT_TELEMETRY
#define USE_SERIAL_TELEMETRY
#endif  // CUSTOM_FD6288_G431
```

---

### 2.2 ADC ZCD 实现

#### 2.2.1 声明（`Mcu/g431/Inc/ADC.h`）

在文件末尾添加：

```c
#ifdef USE_ADC_ZCD
uint16_t readADC_ZCD(uint32_t channel);
#endif
```

#### 2.2.2 GPIO 初始化（`Mcu/g431/Src/ADC.c` — `ADC_Init()` 函数内）

在非 `USE_ADC_1_2` 路径的 `ADC_Init()` 中，在 ADC 校准代码**之前**添加：

```c
#ifdef USE_ADC_ZCD
    /* BEMF A=PA0, B=PA1, C=PA2, 虚拟中性点=PA5 — 配置为模拟输入 */
    GPIO_InitStruct.Pin = LL_GPIO_PIN_0 | LL_GPIO_PIN_1 |
                          LL_GPIO_PIN_2 | LL_GPIO_PIN_5;
    GPIO_InitStruct.Mode = LL_GPIO_MODE_ANALOG;
    GPIO_InitStruct.Pull = LL_GPIO_PULL_NO;
    LL_GPIO_Init(GPIOA, &GPIO_InitStruct);
#endif
```

#### 2.2.3 单次阻塞读取函数（`Mcu/g431/Src/ADC.c` — 文件末尾）

```c
#ifdef USE_ADC_ZCD
uint16_t readADC_ZCD(uint32_t channel)
{
    /* 1. 停止正在进行的 DMA 扫描转换 */
    if (LL_ADC_REG_IsConversionOngoing(ADC1)) {
        LL_ADC_REG_StopConversion(ADC1);
        while (LL_ADC_REG_IsStopConversionOngoing(ADC1)) {}
    }

    /* 2. 切换为单通道、无 DMA 模式 */
    LL_ADC_REG_SetSequencerLength(ADC1, LL_ADC_REG_SEQ_SCAN_DISABLE);
    LL_ADC_REG_SetDMATransfer(ADC1, LL_ADC_REG_DMA_TRANSFER_NONE);
    LL_ADC_REG_SetSequencerRanks(ADC1, LL_ADC_REG_RANK_1, channel);
    LL_ADC_SetChannelSamplingTime(ADC1, channel, LL_ADC_SAMPLINGTIME_12CYCLES_5);
    LL_ADC_SetChannelSingleDiff(ADC1, channel, LL_ADC_SINGLE_ENDED);

    /* 3. 触发转换，阻塞等待 EOC */
    LL_ADC_ClearFlag_EOC(ADC1);
    LL_ADC_REG_StartConversion(ADC1);
    while (!LL_ADC_IsActiveFlag_EOC(ADC1)) {}
    uint16_t result = LL_ADC_REG_ReadConversionData12(ADC1);

    /* 4. 恢复 DMA 扫描序列（温度传感器 + 母线电压） */
    LL_ADC_REG_SetSequencerLength(ADC1, LL_ADC_REG_SEQ_SCAN_ENABLE_2RANKS);
    LL_ADC_REG_SetDMATransfer(ADC1, LL_ADC_REG_DMA_TRANSFER_LIMITED);
    LL_ADC_REG_SetSequencerRanks(ADC1, LL_ADC_REG_RANK_1,
                                 LL_ADC_CHANNEL_TEMPSENSOR_ADC1);
    LL_ADC_SetChannelSamplingTime(ADC1, LL_ADC_CHANNEL_TEMPSENSOR_ADC1,
                                  LL_ADC_SAMPLINGTIME_47CYCLES_5);
    LL_ADC_REG_SetSequencerRanks(ADC1, LL_ADC_REG_RANK_2,
                                 VOLTAGE_ADC_CHANNEL);
    LL_ADC_SetChannelSamplingTime(ADC1, VOLTAGE_ADC_CHANNEL,
                                  LL_ADC_SAMPLINGTIME_47CYCLES_5);
    return result;
}
#endif /* USE_ADC_ZCD */
```

#### 2.2.4 软件比较器实现（`Mcu/g431/Src/comparator.c` — 完整替换）

```c
#include "comparator.h"
#include "common.h"
#include "targets.h"

#ifdef USE_ADC_ZCD
#include "ADC.h"

/* 当前悬空相编号（由 changeCompInput() 从全局 step 变量更新） */
static uint8_t adc_zcd_phase = 0;

/**
 * @brief  读取三相 BEMF 分压，用软件重构的虚拟中性点判断悬空相极性
 * @return 1 = BEMF > 中性点（上升），0 = BEMF < 中性点（下降）
 *
 * 本板无物理虚拟中性点（PA5 只是 1K 下拉空脚，见第十一章），
 * 因此中性点由三相分压读数的平均值 (Va+Vb+Vc)/3 在软件中重构，
 * 不再读取 NEUTRAL_ADC_CHANNEL(PA5)。
 *
 * 换相步骤与悬空相对应关系（BLDC 6步换相）：
 *   Step 1/4 → C 相悬空
 *   Step 2/5 → A 相悬空
 *   Step 3/6 → B 相悬空
 */
uint8_t getCompOutputLevel(void)
{
    uint16_t va = readADC_ZCD(BEMF_A_ADC_CHANNEL);
    uint16_t vb = readADC_ZCD(BEMF_B_ADC_CHANNEL);
    uint16_t vc = readADC_ZCD(BEMF_C_ADC_CHANNEL);
    uint16_t neutral = (uint16_t)(((uint32_t)va + vb + vc) / 3u);
    uint16_t bemf;
    switch (adc_zcd_phase) {
        case 1: case 4: bemf = vc; break;   /* C 相悬空 */
        case 2: case 5: bemf = va; break;   /* A 相悬空 */
        case 3: case 6: bemf = vb; break;   /* B 相悬空 */
        default:        bemf = va; break;
    }
    return (bemf > neutral) ? 1u : 0u;
}


/** 换相后更新悬空相，与全局 step 变量同步 */
void changeCompInput(void)
{
    adc_zcd_phase = step;
}

/* ADC ZCD 不使用硬件比较器中断，以下为空实现 */
void maskPhaseInterrupts(void)  { /* no-op */ }
void enableCompInterrupts(void) { /* no-op */ }

#else  /* ── 原始硬件比较器路径（本板不使用）────────────────── */

COMP_TypeDef* active_COMP = COMP2;
uint32_t current_EXTI_LINE = LL_EXTI_LINE_22;

uint8_t getCompOutputLevel(void)
{
    return LL_COMP_ReadOutputLevel(active_COMP);
}

void maskPhaseInterrupts(void)
{
    LL_EXTI_DisableIT_0_31(current_EXTI_LINE);
}

void enableCompInterrupts(void)
{
    LL_EXTI_ClearFlag_0_31(current_EXTI_LINE);
    LL_EXTI_EnableIT_0_31(current_EXTI_LINE);
}

void changeCompInput(void)
{
    /* 硬件比较器输入切换（略） */
}

#endif /* USE_ADC_ZCD */
```

---

### 2.3 ADC ZCD 调用链与工作原理

AM32 在启动/低速阶段使用 `old_routine=1`（轮询模式），高速稳定后切换到 `old_routine=0`（比较器中断模式）。由于本板无可用的硬件比较器，**始终运行在 `old_routine=1`**，完整调用链如下：

```
主循环 / TIM6 中断（高频）
  └─ if (old_routine && running)          [main.c ~1378行]
       └─ getBemfState()                  [main.c ~803行]
            └─ getCompOutputLevel()       [comparator.c — ADC ZCD实现]
                 ├─ readADC_ZCD(相A/B/C)  [ADC.c — 三相各一次单次阻塞采样, ~0.6μs/相]
                 └─ neutral = (Va+Vb+Vc)/3 [软件重构虚拟中性点，不读 PA5]
                      return (bemf悬空相 > neutral) ? 1 : 0
            ↓
       current_state 与上次对比，电平翻转则 bemfcounter++
            ↓
       if (bemfcounter > min_bemf_counts)
            └─ zcfoundroutine()           [确认过零，触发换相时序]
```

**性能说明：**
- 本板无物理中性点，`getCompOutputLevel()` 采三相 BEMF 分压，用 (Va+Vb+Vc)/3 软件重构基准（见第十一章）
- 每次调用做 3 次单次 ADC 转换（各约 0.6μs），合计约 **1.8μs**，相比读 PA5 的旧实现多一次采样但仍远非瓶颈
- 160MHz 系统时钟下，ADC 时钟频率约 20～40MHz，采样时间 12.5 周期，转换开销可接受
- 如需更高转速，后续可优化为 ADC 定时触发 + DMA 连续扫描，减少 CPU 阻塞时间（路线 C）


---

### 2.4 修复三个运行时隐患

#### 隐患1：硬件比较器干扰 ADC 采样（`Mcu/g431/Src/peripherals.c`）

**问题：** `enableCorePeripherals()` 中无条件调用 `LL_COMP_Enable(COMP1/2)`，在 `USE_ADC_ZCD` 模式下 COMP2 会持续采样 PA1/PA5，与 ADC ZCD 的模拟输入采样冲突，可能导致 BEMF 读数错误。

**关键约束：** EXTI15_10 的 NVIC 和 IMR1 注册是 **DShot 必须项**，与比较器 ZCD 无关。DShot DMA 完成后通过 `EXTI->SWIER1 |= LL_EXTI_LINE_15` 软件触发 EXTI15 中断，进而调用 `processDshot()`。若将 NVIC+IMR1 也放入 `#ifndef USE_ADC_ZCD`，则 `USE_ADC_ZCD` 模式下 DShot 完全失效。

**正确修复：** 只屏蔽 COMP Enable，NVIC/IMR1 无条件保留：

```c
#ifndef USE_ADC_ZCD
    /* 硬件比较器 ZCD：使能 COMP1/2（ADC ZCD 模式下跳过，
     * 避免 COMP2 持续采样 PA1/PA5 干扰 ADC ZCD 采样） */
    LL_COMP_Enable(COMP2);
    LL_COMP_Enable(COMP1);
#endif /* USE_ADC_ZCD */

    /* EXTI15_10 NVIC 和 IMR1 无论是否使用 ADC ZCD 都必须使能：
     * DShot DMA 完成后通过 EXTI->SWIER1 |= LL_EXTI_LINE_15 触发
     * EXTI15_10_IRQHandler → processDshot()，与比较器 ZCD 无关 */
    NVIC_SetPriority(EXTI15_10_IRQn, 2);
    NVIC_EnableIRQ(EXTI15_10_IRQn);
    EXTI->IMR1 |= (1 << 15);
```

> ⚠️ **常见错误**：将 NVIC+IMR1 也放入 `#ifndef USE_ADC_ZCD` 块内，会导致 `USE_ADC_ZCD` 模式下 DShot 协议完全失效（电调收不到油门指令）。两者必须分开处理。

#### 隐患2：DMA 背景扫描无法自动重启（`Mcu/g431/Src/ADC.c`）

**问题：** ADC1 配置为 `DMA_TRANSFER_LIMITED` 模式：每次完成一个完整序列后 DMA 停止，需要软件再次触发。`readADC_ZCD()` 恢复 DMA 序列配置后没有重新调用 `StartConversion()`，导致温度/电压后台采样永久停止，`ADC_raw_temp` 和 `ADC_raw_volts` 固定为首次值。

**修复：** 在恢复序列配置后补充启动：

```c
    /* 恢复 DMA 扫描序列配置... */
    LL_ADC_REG_SetSequencerLength(ADC1, LL_ADC_REG_SEQ_SCAN_ENABLE_2RANKS);
    LL_ADC_REG_SetDMATransfer(ADC1, LL_ADC_REG_DMA_TRANSFER_LIMITED);
    // ... rank1/rank2 配置 ...

    /* 重新触发 DMA 扫描转换 */
    LL_ADC_REG_StartConversion(ADC1);   // ← 新增这行
```

#### 隐患3：`old_routine=0` 切换导致系统挂起（`Mcu/g431/Src/comparator.c`）

**问题：** `zcfoundroutine()` 在找到稳定过零后会设置 `old_routine=0` 并调用 `enableCompInterrupts()`，系统随后进入等待硬件比较器中断的路径。由于本板没有可用硬件比较器，该中断永远不会触发，换相循环永久挂起。

**修复：** `enableCompInterrupts()` 强制将 `old_routine` 设回 1：

```c
extern char old_routine;   /* defined in main.c */

void enableCompInterrupts(void)
{
    /* 无硬件比较器：拒绝切换到中断路径，强制保持 ADC 轮询模式 */
    old_routine = 1;
}
```

---

#### 隐患4：⚡ **电机完全不转的根本原因** — 首次换相永远不触发（`Src/main.c`）

**现象：** AM32 Configurator 能通过 PB5（DShot 接口）正常连接，证明固件运行正常、DShot arming 流程完全正确，但无论油门推多高电机始终无任何反应、无电流变化。

**根因分析：**

AM32 有两种换相路径，由 `old_routine` 标志控制：

| 路径 | 条件 | 机制 |
|------|------|------|
| `old_routine = 1` | 启动/低速轮询阶段 | 主循环每次采样 BEMF，检测过零后触发换相 |
| `old_routine = 0` | 高速稳定阶段 | 硬件比较器 EXTI 中断触发换相 |

问题出在 `Src/main.c` 第1166行的启动入口：

```c
// main.c 启动逻辑（问题代码）
if (running == 0) {
    allOff();
    if (!old_routine) {      // ← old_routine 初始值为 1，条件永远为 false！
        startMotor();        // ← 第一次 commutate() 在这里，被永远跳过！
    }
    running = 1;             // running 被置 1，但从未执行任何换相！
    last_duty_cycle = min_startup_duty;
}
```

**完整 Bug 链：**

```
1. old_routine = 1（全局初始值，main.c 第49行）
        ↓
2. enableCompInterrupts() [USE_ADC_ZCD 下是空函数，不改变 old_routine]
   → 隐患3修复后：强制 old_routine = 1，更彻底锁死
        ↓
3. 收到 DShot 油门 → setInput() → if(running == 0)
        ↓
4. if (!old_routine) → 1 == 1 为 false → startMotor() 被跳过
        ↓
5. running = 1 被设置，但 commutate() 从未被调用
        ↓
6. old_routine=1 轮询路径（main.c ~1378行）需要 running=1 + 已有 BEMF 信号
   → 但第一个换相步骤从未发生 → 永远没有 PWM 输出 → 永远没有 BEMF → 死循环
        ↓
7. 电机完全静止，无任何电流变化
```

> **为什么硬件比较器目标能正常工作？**  
> 硬件比较器目标的 `enableCompInterrupts()` 不强制修改 `old_routine`。`zcfoundroutine()` 会在 `zero_crosses > 2`（`NO_POLLING_START` 模式）时将 `old_routine` 置 0，此时 `startMotor()` 才被调用。  
> 但 `USE_ADC_ZCD` 目标的 `enableCompInterrupts()` 强制 `old_routine = 1`，使得这条切换路径永远无法到达，`startMotor()` 永远无法被调用。

**修复方案（`Src/main.c`）：**

在 `running == 0` 的启动入口，当 `old_routine == 1` 时直接调用 `commutate()` 触发首次换相，绕过 `startMotor()` 的条件门控：

```c
if (running == 0) {
    allOff();
    if (!old_routine) {
        startMotor();
    } else {
        /* USE_ADC_ZCD forces old_routine=1 permanently.
         * startMotor() is gated on !old_routine, so it would
         * never be called. Call commutate() directly to fire
         * the first commutation step and get the COM_TIMER
         * rolling for the old_routine polling path. */
        commutate();
        commutation_interval = 10000;
        SET_INTERVAL_TIMER_COUNT(5000);
    }
    running = 1;
    last_duty_cycle = min_startup_duty;
}
```

**修复逻辑说明：**
- `commutate()` 设置第一个换相步骤（调用 `comStep(step)`），激活 PWM 输出
- `commutation_interval = 10000` 设置初始换相间隔（约 5ms，对应约 200 Hz 电角频率）
- `SET_INTERVAL_TIMER_COUNT(5000)` 设置定时器初始值，与 `startMotor()` 内部逻辑完全一致
- 此后 `old_routine=1` 轮询路径接管：每次 `getBemfState()` 检测到 BEMF 过零就推进换相

> ✅ **此修复是电机能否转动的关键**。上述所有其他修复（隐患1~3）均为正确性保障，此处修复才是"从0到1"的决定性变更。

---

### 2.5 修复上游遗留 Bug（`Mcu/g431/Src/peripherals.c`）

**问题：** `MX_TIM3_Init()` 函数开头无条件声明了 `TIM_InitStruct` 和 `GPIO_InitStruct`，但这两个变量只在 `#ifdef USE_TIMER_3_CHANNEL_1` / `#ifdef USE_TIMER_3_CHANNEL_2` 分支内使用。当两个宏均未定义时，GCC 13 的 `-Werror=unused-variable` 将此视为错误，导致所有 G431 目标编译中断。

**修复：** 将变量声明移入各自的 `#ifdef` 作用域块内：

```c
void MX_TIM3_Init(void)
{
    /* 外设时钟使能（无论哪个通道都需要） */
    LL_APB1_GRP1_EnableClock(LL_APB1_GRP1_PERIPH_TIM3);
    LL_AHB2_GRP1_EnableClock(LL_AHB2_GRP1_PERIPH_GPIOB);

#ifdef USE_TIMER_3_CHANNEL_1
    {
    LL_TIM_InitTypeDef  TIM_InitStruct  = { 0 };   // ← 移入块作用域
    LL_GPIO_InitTypeDef GPIO_InitStruct = { 0 };
    // ... PB4 / TIM3_CH1 配置 ...
    }
#endif

#ifdef USE_TIMER_3_CHANNEL_2
    {
    LL_TIM_InitTypeDef  TIM_InitStruct  = { 0 };   // ← 移入块作用域
    LL_GPIO_InitTypeDef GPIO_InitStruct = { 0 };
    // ... PB5 / TIM3_CH2 配置 ...
    }
#endif
}
```

---

### 2.6 编译主固件

```bash
cd /mnt/c/Users/nxf75465/Desktop/AM32-main
make TARGET=CUSTOM_FD6288_G431 ARM_SDK_PREFIX=/usr/bin/arm-none-eabi-
```

> **注意：** Makefile 实际会编译所有目标，`TARGET=` 参数仅影响 MCU 类型选择，不做目标过滤。RISC-V 目标（`AIRBOT_V203`）因缺少 RISC-V 工具链而报错属正常现象，不影响 ARM 目标产物。

**编译结果（`arm-none-eabi-size` 输出）：**

```
   text    data     bss     dec     hex  filename
  24292     984    2712   27988    6d54  AM32_CUSTOM_FD6288_G431_2.20.elf
```

| 区域 | 使用 | 芯片总量 | 占比 |
|------|------|----------|------|
| Flash (text+data) | ~25 KB | 128 KB | ~19.5% |
| RAM (data+bss) | ~3.7 KB | 32 KB | ~11.5% |

**输出文件：**
```
obj/AM32_CUSTOM_FD6288_G431_2.20.bin   (59392 字节 ≈ 58 KB)
obj/AM32_CUSTOM_FD6288_G431_2.20.elf
```

**ELF 符号验证（ADC ZCD 均已链接）：**

```
080052a4 T readADC_ZCD
080054f8 T getCompOutputLevel
08005544 T changeCompInput
20000858 b adc_zcd_phase
```

---

## 三、Bootloader 移植（AM32-bootloader）

### 3.1 添加 PB5 引脚支持

**文件：`bootloader/main.c`**

在 `#elif defined(USE_PB4)` 块之后插入：

```c
#elif defined(USE_PB5)
#define input_pin     GPIO_PIN(5)
#define input_port    GPIOB
#define PIN_NUMBER    5
#define PORT_LETTER   1
```

### 3.2 注册 PB5 目标 + 修复 FLASH 溢出

**文件：`Makefile`（AM32-bootloader）**

```makefile
# 添加 PB5 到支持的引脚列表
BOOTLOADER_PINS = PB4 PA2 PA6 PA15 PA0 PB2 PB5

# 添加 -flto 解决 GCC 13 代码体积增大导致的 FLASH 溢出
CFLAGS_BASE := -fsingle-precision-constant -fomit-frame-pointer -ffast-math --specs=nosys.specs
CFLAGS_BASE += -I$(MAIN_INC_DIR) -g3 -Os -ffunction-sections -funsigned-char -flto
```

> **原因：** GCC 13 生成的代码比旧版本大约 430 字节，超出 bootloader 的 4 KB - 32 B = 4064 B FLASH 限制。`-flto`（链接时优化）将代码压缩至 3812 B（93.80%），恢复正常。

### 3.3 编译 Bootloader

```bash
cd /mnt/c/Users/nxf75465/Desktop/AM32-bootloader-master
make AM32_G431_BOOTLOADER_PB5 ARM_SDK_PREFIX=/usr/bin/arm-none-eabi-
```

**编译结果：**

| 区域 | 使用 | 总量 | 占比 |
|------|------|------|------|
| FLASH | 3812 B | 4064 B | 93.80% |
| RAM | 2096 B | 4 KB | 51.17% |

**输出文件：**
```
obj/AM32_G431_BOOTLOADER_PB5_V17.bin   (4.0 KB)
obj/AM32_G431_BOOTLOADER_PB5_V17.hex   (11 KB)
```

---

## 四、烧录说明

### Flash 地址布局

```
0x08000000  ┌────────────────────────────┐
            │  Bootloader（4 KB）        │  ← AM32_G431_BOOTLOADER_PB5_V17.bin
0x08001000  ├────────────────────────────┤
            │  主固件（~58 KB）          │  ← AM32_CUSTOM_FD6288_G431_2.20.bin
            │  (EEPROM 在末尾 2 KB)      │
0x0800FFFF  └────────────────────────────┘
```

---

### 第一步：通过 SWD 烧录 Bootloader（只需一次）

```bash
# 方法一：st-flash
st-flash write obj/AM32_G431_BOOTLOADER_PB5_V17.bin 0x08000000

# 方法二：STM32CubeProgrammer CLI
STM32_Programmer_CLI -c port=SWD \
  -w obj/AM32_G431_BOOTLOADER_PB5_V17.bin 0x08000000 -v
```

烧录后重启，若信号线（PB5）悬空，bootloader 会等待通信；若 PB5 被拉低（无信号），则自动跳转到主固件地址。

---

### 第二步（方法 A）：通过地面站/配置工具烧录主固件

烧录 bootloader 后，可使用 **AM32 Configurator** 通过 DSHOT 信号线（PB5）在线更新主固件，无需 SWD 调试器。

**步骤：**
1. 用信号线将 PB5 连接至电脑（通过 USB-串口适配器，RX 连 PB5，3.3V 电平）
2. 打开 [AM32 Configurator](https://github.com/AlkaMotors/AM32-Configurator)
3. 选择对应串口，点击 **"Flash Firmware"**
4. 选择固件文件：`obj/AM32_CUSTOM_FD6288_G431_2.20.bin`
5. 等待进度条完成，工具会自动验证 CRC

> **注意：** Bootloader 协议波特率为 **19200 bps**，8N1，非反相 UART。

---

### 第二步（方法 B）：通过 SWD 直接烧录主固件

```bash
# 方法一：st-flash（务必烧录到 0x08001000！）
st-flash write obj/AM32_CUSTOM_FD6288_G431_2.20.bin 0x08001000

# 方法二：STM32CubeProgrammer CLI
STM32_Programmer_CLI -c port=SWD \
  -w obj/AM32_CUSTOM_FD6288_G431_2.20.bin 0x08001000 -v

# 方法三：OpenOCD
openocd -f interface/stlink.cfg -f target/stm32g4x.cfg \
  -c "program obj/AM32_CUSTOM_FD6288_G431_2.20.bin 0x08001000 verify reset exit"
```

> ⚠️ **务必烧录到 `0x08001000` 而不是 `0x08000000`**，否则会覆盖 bootloader！

---

### 全新芯片一次性烧录（bootloader + 主固件）

```bash
# STM32CubeProgrammer 合并烧录
STM32_Programmer_CLI -c port=SWD \
  -w obj/AM32_G431_BOOTLOADER_PB5_V17.bin 0x08000000 \
  -w obj/AM32_CUSTOM_FD6288_G431_2.20.bin 0x08001000 \
  -v -rst
```

---

## 五、首次上电 EEPROM 配置

AM32 主固件首次运行时，EEPROM 未初始化（第一字节不为 `0x01`），bootloader 会拒绝跳转到主固件，持续等待配置工具连接。

使用 AM32 Configurator 连接后：
1. 确认 ESC 目标识别为 `FD6288-G431`
2. 保存默认配置（将初始化 EEPROM）
3. 断电重启后，bootloader 检测到 EEPROM 有效 → 自动跳转主固件

---

## 六、修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| `AM32-main/Inc/targets.h` | 新增 `CUSTOM_FD6288_G431` target 块，含 `USE_ADC_ZCD`、BEMF/中性点/电压 ADC 通道定义、COMP 占位符、所有引脚定义 |
| `AM32-main/Mcu/g431/Inc/ADC.h` | `#ifdef USE_ADC_ZCD` 下添加 `readADC_ZCD(uint32_t channel)` 声明 |
| `AM32-main/Mcu/g431/Src/ADC.c` | `ADC_Init()` 中添加 BEMF/中性点 GPIO 模拟输入初始化；末尾添加 `readADC_ZCD()` 实现（停DMA→单次采样→恢复DMA） |
| `AM32-main/Mcu/g431/Src/comparator.c` | 完整替换为 ADC ZCD 软件路径：`getCompOutputLevel()`（ADC比较）、`changeCompInput()`（同步step）、`maskPhaseInterrupts()`/`enableCompInterrupts()`（空实现） |
| `AM32-main/Mcu/g431/Src/peripherals.c` | 修复上游 Bug：`MX_TIM3_Init()` 中将 `TIM_InitStruct`/`GPIO_InitStruct` 声明移入各自 `#ifdef` 作用域，消除 `-Werror=unused-variable` 编译错误；`enableCorePeripherals()` 中用 `#ifndef USE_ADC_ZCD` 包裹 COMP Enable 和 EXTI 中断使能 |
| `AM32-bootloader-master/bootloader/main.c` | 新增 `USE_PB5` 引脚定义块 |
| `AM32-bootloader-master/Makefile` | 注册 `PB5` 目标、添加 `-flto` 解决 GCC 13 FLASH 溢出 |

---

## 七、调试验证换相步骤

> 目标：在上电运行前，验证 `step → 悬空相 → ADC 通道` 映射关系正确，以及 `getCompOutputLevel()` 能够正确判断 BEMF 极性。

---

### 7.1 方法一：SWD Live Watch（推荐，零侵入）

使用 **STM32CubeIDE** 或 **OpenOCD + GDB** 的实时变量监视，无需修改代码。

**操作步骤：**

1. 用 SWD 连接目标板，加载 ELF 文件  
   ```bash
   # OpenOCD 后台启动
   openocd -f interface/stlink.cfg -f target/stm32g4x.cfg &
   
   # GDB 连接
   arm-none-eabi-gdb obj/AM32_CUSTOM_FD6288_G431_2.20.elf
   (gdb) target remote :3333
   (gdb) monitor reset halt
   (gdb) load
   (gdb) monitor reset run
   ```

2. 在 GDB 中监视关键变量（电机**低速手动拨动**状态下）：
   ```
   (gdb) watch step
   (gdb) watch adc_zcd_phase          # comparator.c 中的 static 变量
   (gdb) print step
   (gdb) print adc_zcd_phase
   ```

3. **验证映射表**：每次 `step` 改变后，`adc_zcd_phase` 应跟随：

   | step | adc_zcd_phase | 期望悬空相 | ADC 通道 |
   |------|--------------|------------|----------|
   | 1    | 1            | C          | CH3 (PA2) |
   | 2    | 2            | A          | CH1 (PA0) |
   | 3    | 3            | B          | CH2 (PA1) |
   | 4    | 4            | C          | CH3 (PA2) |
   | 5    | 5            | A          | CH1 (PA0) |
   | 6    | 6            | B          | CH2 (PA1) |

> ⚠️ `adc_zcd_phase` 是 `static` 变量，GDB 中需要用完整作用域访问：  
> `(gdb) print 'comparator.c'::adc_zcd_phase`

---

### 7.2 方法二：临时调试串口输出

在 `comparator.c` 的 `getCompOutputLevel()` 中临时加入串口打印（调试完成后删除）：

```c
#include <stdio.h>      // printf → 重定向到 USART1

uint8_t getCompOutputLevel(void)
{
    uint16_t va = readADC_ZCD(BEMF_A_ADC_CHANNEL);
    uint16_t vb = readADC_ZCD(BEMF_B_ADC_CHANNEL);
    uint16_t vc = readADC_ZCD(BEMF_C_ADC_CHANNEL);
    uint16_t neutral = (uint16_t)(((uint32_t)va + vb + vc) / 3u);
    uint16_t bemf;
    switch (adc_zcd_phase) {
        case 1: case 4: bemf = vc; break;   /* C 相悬空 */
        case 2: case 5: bemf = va; break;   /* A 相悬空 */
        case 3: case 6: bemf = vb; break;   /* B 相悬空 */
        default:        bemf = va; break;
    }
    uint8_t result = (bemf > neutral) ? 1u : 0u;

    /* ── 临时调试输出（降低调用频率：只在 step 变化时打印）── */
    static uint8_t last_step = 0xFF;
    if (adc_zcd_phase != last_step) {
        last_step = adc_zcd_phase;
        printf("STEP=%u  Va=%4u Vb=%4u Vc=%4u  BEMF=%4u  NEUTRAL=%4u  OUT=%u\r\n",
               adc_zcd_phase, va, vb, vc, bemf, neutral, result);
    }
    return result;
}
```


**串口参数：** USART1 (PB6)，115200-8N1，与 PC 串口工具连接  
**正常输出示例（低速缓慢旋转，NEUTRAL 为软件计算的 (Va+Vb+Vc)/3）：**
```
STEP=1  Va=1024 Vb=1150 Vc= 892  BEMF= 892  NEUTRAL=1022  OUT=0
STEP=2  Va=1187 Vb= 980 Vc=1050  BEMF=1187  NEUTRAL=1072  OUT=1
STEP=3  Va=1008 Vb= 756 Vc=1100  BEMF= 756  NEUTRAL= 954  OUT=0
STEP=4  Va= 990 Vb=1060 Vc=2108  BEMF=2108  NEUTRAL=1386  OUT=1
STEP=5  Va= 623 Vb=1030 Vc=1010  BEMF= 623  NEUTRAL= 887  OUT=0
STEP=6  Va=1024 Vb=1892 Vc= 980  BEMF=1892  NEUTRAL=1298  OUT=1
```

**异常判断：**
- `Va/Vb/Vc` 有一路始终 `≈0` 或 `≈4095`：对应 BEMF 通道映射错误或分压断路，检查 `BEMF_x_ADC_CHANNEL` 定义与硬件
- 三相读数彼此差异极小、`NEUTRAL` 几乎不随 step 变化：电机未真正驱动（无 PWM 输出），先查换相/PWM
- `BEMF ≈ NEUTRAL`（差值 < 50）：采样落在续流期或换相后延迟不足，考虑路线 C 的 blanking


---

### 7.3 方法三：示波器验证 BEMF 与 ADC 采样时序

用双通道示波器同时测量：

| 通道 | 测量点 | 说明 |
|------|--------|------|
| CH1 | 悬空相（PA0/PA1/PA2 对应的分压输出） | BEMF 梯形波 |
| CH2 | 任一非悬空相分压输出 或 母线中点 | 参考电平（本板无 PA5 物理中性点，软件用 (Va+Vb+Vc)/3 代替） |

> 说明：本板 PA5 是空脚（见第十一章），示波器上无法直接量到"物理中性点"。软件基准是三相分压平均值 (Va+Vb+Vc)/3；观察过零时可用任一非悬空相分压或母线中点作为近似参考电平。

**正常波形特征：**
- CH1 每个换相周期内出现一次过零（穿越软件中性点电平 (Va+Vb+Vc)/3）

- 过零点应出现在换相后约 **30° 电角度**（即下次换相时刻的 1/2 处）
- 若过零总是发生在换相刚结束（< 5% 周期内），说明换相步骤顺序错误（相序反转）

**相序反转的判断与修复：**
```
现象：电机启动后立刻堵转、发热，或反向旋转
原因：step 与悬空相映射相对于实际电机绕组顺序相反

修复方案（在 comparator.c 中调换两相）：
    case 1: case 4: bemf_ch = BEMF_A_ADC_CHANNEL; break;  // C↔A 互换
    case 2: case 5: bemf_ch = BEMF_C_ADC_CHANNEL; break;
    case 3: case 6: bemf_ch = BEMF_B_ADC_CHANNEL; break;
```

---

### 7.4 方法四：静态注入验证 ADC 比较逻辑

在**不旋转电机**的情况下，用已知电压验证 ADC 读值和比较逻辑。

**操作步骤：**

> 注意：本板已不再读 PA5，中性点由软件 (Va+Vb+Vc)/3 计算。因此静态注入时中性点由 PA0/PA1/PA2 三路读数决定，把非测试相 (PA1/PA2) 都固定在 VCC/2，则 neutral≈VCC/2，再改变被测相 PA0 即可验证比较逻辑。

1. 断开电机三相连接（防止误驱动）
2. 在 PA0（BEMF A）接一个分压：VCC→10kΩ→PA0→10kΩ→GND，使 PA0 ≈ VCC/2 ≈ 1.65V
3. 把 PA1、PA2 也接 VCC/2 参考（同上分压），使软件计算的 neutral ≈ VCC/2
4. 修改 `adc_zcd_phase = 2`（强制选择 BEMF A 通道），调用 `getCompOutputLevel()`
5. 将 PA0 改为接近 VCC（3.0V）→ 期望返回 `1`；接近 GND（0.3V）→ 期望返回 `0`


```c
/* 临时测试代码，加在 main() 的初始化循环中 */
extern uint8_t getCompOutputLevel(void);
extern uint8_t adc_zcd_phase;

adc_zcd_phase = 2;   // 强制测 BEMF_A (PA0, CH1)
while (1) {
    uint8_t level = getCompOutputLevel();
    HAL_Delay(100);
    // 用 SWD watch 或串口观察 level 值
}
```

---

### 7.5 验证通过的判定标准

在进行实机首次通电前，需确认以下所有检查项：

- [ ] **GDB 映射表验证**：手动拨动电机，`step` 每变化一次，`adc_zcd_phase` 同步更新，且映射关系与表 7.1 一致
- [ ] **ADC 读值合理性**：`BEMF` 和 `NEUTRAL` 均在 200～3800 之间（不饱和、不截断）
- [ ] **中性点稳定性**：`NEUTRAL` 读值在电机旋转过程中波动 < ±100 counts
- [ ] **过零极性交替**：每个换相步骤的 `getCompOutputLevel()` 返回值与上一步相反（1→0→1→0…）
- [ ] **静态注入测试**：PA0 接高压返回 `1`，接低压返回 `0`（验证比较逻辑无反转）
- [ ] **示波器确认**：BEMF 过零点发生在换相后 ~30% 至 ~70% 周期区间内

所有检查项通过后，即可接入电机进行低油门（< 5%）首次通电测试。

---

## 八、AM32 Configurator 兼容性修复（2026-05-12）

### 8.1 问题根因分析

上板调试发现以下现象，最终定位到同一根因：

| 现象 | 根因 |
|------|------|
| Configurator 连入后显示 "Fresh EEPROM" | G431 signature `0x3734` 不在 Configurator `mcu.ts` variants 表中 |
| 通过 Configurator 写固件失败（write cmd fail） | `writeHex()` 依赖 variants 表获取 `flashOffset`/`pageSize`/`firmwareStart`，未知 signature 直接抛异常 |
| 写参数能成功，写固件不能 | `writeSettings()` 通过 `ADDRESS_MAGIC_EEPROM(0x20)` 魔术地址由 bootloader 映射到真实 EEPROM 地址，绕过了 variant 查询 |
| 无自检音、电机不转 | bootloader 检查 `0x0800F800[0] == 0x01` 决定是否跳转，该地址出厂全 `0xFF`，主固件从未运行 |

**EEPROM 地址说明**：

G431 bootloader 以 `BOARD_FLASH_SIZE=64` 编译，`EEPROM_START_ADD = 0x0800F800`，**不是** `0x7C00`（那是 F051/32K 配置）。手动用 SWD 向 `0x7C00` 写数据对 G431 无效。

**Configurator signature 来源**：

```c
// bootloader devinfo 结构（原始）
.deviceInfo = {'4','7','1', PIN_CODE, FLASH_SIZE_CODE, ...}
//              0x34  0x37
// Configurator 读取：signature = (params[1]<<8)|params[0] = 0x3734
// mcu.ts variants 表只有 0x1F06(F051) 和 0x3506(ARM64K)，无 0x3734 → 报错
```

---

### 8.2 解决方案：修改 bootloader devinfo 伪装为 ARM64K

**文件：`AM32-bootloader-master/bootloader/main.c`**

ARM64K（signature `0x3506`）在 `mcu.ts` 中的 `eeprom_offset = '0xF800'`，与 G431 的 `EEPROM_START_ADD = 0x0800F800` 完全一致，可以安全伪装。

修改第202行 `devinfo.deviceInfo` 前两个字节：

```c
// 修改前（G431 原始 signature 0x3734）：
.deviceInfo = {'4','7','1', PIN_CODE, FLASH_SIZE_CODE, 0x06, 0x06, BOOTLOADER_PROTOCOL_VERSION, 0x30}

// 修改后（伪装成 ARM64K，signature 0x3506）：
/* Spoof signature as ARM64K (0x3506) so AM32 Configurator recognises this
 * G431 target. Configurator reads: signature = (params[1]<<8)|params[0]
 * ARM64K: 0x3506 → params[0]=0x06, params[1]=0x35
 * eeprom_offset in mcu.ts for ARM64K is 0xF800, matching G431 EEPROM_START_ADD */
.deviceInfo = {0x06, 0x35, '1', PIN_CODE, FLASH_SIZE_CODE, 0x06, 0x06, BOOTLOADER_PROTOCOL_VERSION, 0x30}
```

**重新编译 bootloader**：

```bash
cd AM32-bootloader-master
make AM32_G431_BOOTLOADER_PB5 ARM_SDK_PREFIX=/usr/bin/arm-none-eabi-
# 产物：obj/AM32_G431_BOOTLOADER_PB5_V17.hex
# FLASH 使用：3812B / 4064B = 93.8%
```

**验证 signature**（已验证）：
```
devinfo at offset 0x0FE0
deviceInfo bytes: [0x06, 0x35, 0x31, 0x15, 0x35, 0x06, 0x06, 0x02, 0x30]
signature = 0x3506 ✅ (ARM64K)
```

---

### 8.3 主固件 `old_routine` 启动修复

**文件：`AM32-main/Src/main.c`**（`running==0` 启动入口，约第1165行）

`USE_ADC_ZCD` 强制 `old_routine=1`，而原代码启动路径 `if (!old_routine) { startMotor(); }` 永远不执行。修复为直接调用 `commutate()`：

```c
if (running == 0) {
    allOff();
    if (!old_routine) {
        startMotor();
    } else {
        /* USE_ADC_ZCD forces old_routine=1 permanently.
         * Call commutate() directly to fire the first commutation step. */
        commutate();
        commutation_interval = 10000;
        SET_INTERVAL_TIMER_COUNT(5000);
    }
    running = 1;
    last_duty_cycle = min_startup_duty;
}
```

**文件：`AM32-main/Mcu/g431/Src/comparator.c`**（`enableCompInterrupts()` 末尾）

防止速度达到阈值后切换到硬件比较器中断模式：

```c
extern char old_routine;
// 函数末尾强制维持 ADC 轮询模式
old_routine = 1;
```

---

### 8.4 紧急 EEPROM 初始化（SWD 直写）

若 Configurator 仍无法正常工作，可用 SWD 直接向 `0x0800F800` 写入预置参数：

**预置 bin 文件**：`AM32-G431-EEPROM-F800.bin`（192字节，位于桌面）

| offset | 值 | 含义 |
|--------|-----|------|
| `[0]` | `0x01` | bootloader 跳转标志（必须） |
| `[1]` | `0x03` | eeprom_version = 3 |
| `[2]` | `0x11` | bootloader revision = 17 |
| `[3]` | `0x02` | version.major = 2（消除 Fresh EEPROM） |
| `[4]` | `0x14` | version.minor = 20 |
| `[20]` | `0x01` | comp_pwm = 1（互补 PWM） |
| `[24]` | `0x18` | pwm_frequency = 24kHz |
| `[43]` | `0xFF` | 温度保护禁用 |

**烧录命令**：

```bash
# STM32CubeProgrammer
STM32_Programmer_CLI -c port=SWD \
  -w AM32-G431-EEPROM-F800.bin 0x0800F800 -v

# OpenOCD
openocd -f interface/stlink.cfg -f target/stm32g4x.cfg \
  -c "init; program AM32-G431-EEPROM-F800.bin 0x0800F800 verify; exit"
```

写入后重启，bootloader 检测到 `0x0800F800[0]=0x01` 即跳转主固件，自检音和电机控制恢复正常。

---

### 8.5 修改文件汇总（本次新增）

| 文件 | 修改内容 |
|------|----------|
| ~~`AM32-bootloader-master/bootloader/main.c`~~ | ~~`devinfo.deviceInfo[0]=0x06, [1]=0x35`，伪装 signature 为 ARM64K(0x3506)~~ **⚠️ 此修改会导致严重问题：板子无法正常启动，且 Configurator 进不去（地面站无法连接）。已回滚到原始 `{'4','7','1',...}`。不要尝试此修改！** |
| `AM32-main/Src/main.c`（启动入口） | `old_routine=1` 时直接调用 `commutate()` 代替跳过的 `startMotor()` |
| `AM32-main/Mcu/g431/Src/comparator.c` | `enableCompInterrupts()` 末尾强制 `old_routine=1`，防止切换到硬件比较器模式 |
| `AM32-G431-EEPROM-F800.bin`（新增） | 192字节预置 EEPROM 数据，可直接 SWD 烧录到 `0x0800F800` |

---

## 九、PB6（USART1 TX）串口遥测无输出的修复

### 9.1 问题现象

遥测串口 PB6（USART1 TX）完全没有信号输出，用示波器/逻辑分析仪观察不到任何电平变化。

### 9.2 根因分析

`Mcu/g431/Src/serial_telemetry.c` 中存在两个问题：

#### 问题1：半双工模式（`LL_USART_ConfigHalfDuplexMode`）

`telem_UART_Init()` 中调用了 `LL_USART_ConfigHalfDuplexMode(USART1)`，将 USART1 配置为半双工模式。在该模式下：
- TX 和 RX 共用同一引脚（PB6），由 `TE` 位控制引脚方向
- `TE=1` 时引脚为输出（发送），`TE=0` 时引脚为输入（接收）

由于遥测是单向发送，不需要半双工模式，使用标准全双工 `LL_USART_DIRECTION_TX`（仅发送）即可。

#### 问题2：`send_telem_DMA()` 中发送被立即终止

```c
void send_telem_DMA(uint8_t bytes)
{
     LL_USART_SetTransferDirection(USART1, LL_USART_DIRECTION_TX); // ← 使能发送器
     LL_DMA_DisableChannel(DMA1, LL_DMA_CHANNEL_3);
     LL_DMA_SetDataLength(DMA1, LL_DMA_CHANNEL_3, bytes);
     LL_USART_EnableDMAReq_TX(USART1);
    LL_DMA_EnableChannel(DMA1, LL_DMA_CHANNEL_3);
    LL_USART_SetTransferDirection(USART1, LL_USART_DIRECTION_RX); // ← 立即关闭发送器！
}
```

第88行在 DMA 启动后立即调用 `LL_USART_SetTransferDirection(USART1, LL_USART_DIRECTION_RX)`，这会清除 USART_CR1 的 `TE` 位，关闭发送器。即使之前已写入 DR 的数据会被发送完成，后续 DMA 写入 DR 的字节也不会被移出。

### 9.3 修复方案

**文件：`AM32-main/Mcu/g431/Src/serial_telemetry.c`**

#### 修改1：初始化为纯 TX 模式，去掉半双工

```c
// 修改前：
USART_InitStruct.TransferDirection = LL_USART_DIRECTION_TX_RX;
// ...
LL_USART_ConfigHalfDuplexMode(USART1);

// 修改后：
USART_InitStruct.TransferDirection = LL_USART_DIRECTION_TX;
// ...（去掉 LL_USART_ConfigHalfDuplexMode）
```

#### 修改2：`send_telem_DMA()` 中去掉方向切换

```c
// 修改前：
void send_telem_DMA(uint8_t bytes)
{
     LL_USART_SetTransferDirection(USART1, LL_USART_DIRECTION_TX);
     LL_DMA_DisableChannel(DMA1, LL_DMA_CHANNEL_3);
     LL_DMA_SetDataLength(DMA1, LL_DMA_CHANNEL_3, bytes);
     LL_USART_EnableDMAReq_TX(USART1);
    LL_DMA_EnableChannel(DMA1, LL_DMA_CHANNEL_3);
    LL_USART_SetTransferDirection(USART1, LL_USART_DIRECTION_RX); // ← 删掉这行
}

// 修改后：
void send_telem_DMA(uint8_t bytes)
{
     LL_DMA_DisableChannel(DMA1, LL_DMA_CHANNEL_3);
     LL_DMA_SetDataLength(DMA1, LL_DMA_CHANNEL_3, bytes);
     LL_USART_EnableDMAReq_TX(USART1);
    LL_DMA_EnableChannel(DMA1, LL_DMA_CHANNEL_3);
}
```

USART 初始化时方向已固定为 `TX`，DMA 发送时不再需要切换方向。

### 9.4 修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| ~~`AM32-main/Mcu/g431/Src/serial_telemetry.c`~~ | ~~`telem_UART_Init()`: 去掉半双工模式，方向改为纯 TX；`send_telem_DMA()`: 去掉发送后的 RX 方向切换~~ **不需要。bootloader 修好后原版串口代码正常工作。最终方案：串口代码保持原样，不做任何修改。** |

---

## 十、Bootloader 修复

### 10.1 问题现象

Bootloader 不跳转到主固件。示波器看 PB6 无输出，Configurator 可连接但无法刷写固件。

### 10.2 根因分析

`bootloader/main.c` 第 64-70 行定义了主固件的起始地址偏移：

```c
#ifndef FIRMWARE_RELATIVE_START
#if defined(DRONECAN_SUPPORT) || defined(MCXA153)
#define FIRMWARE_RELATIVE_START 0x4000    // 32KB offset
#else
#define FIRMWARE_RELATIVE_START 0x1000    // 4KB offset
#endif
#endif
```

`Makefile` 始终通过 `-DDRONECAN_SUPPORT=0` 或 `-DDRONECAN_SUPPORT=1` 定义该宏。C 预处理器中 `defined(DRONECAN_SUPPORT)` 无论值为 0 还是 1 均为 `true`。因此 `FIRMWARE_RELATIVE_START` 被设为 `0x4000`，bootloader 跳转到 `0x08004000`（空白区域），CPU 执行无效代码。

### 10.3 修复

**文件：`bootloader/main.c`，仅修改第 65 行：**

```c
// 改前：
#if defined(DRONECAN_SUPPORT) || defined(MCXA153)

// 改后：
#if DRONECAN_SUPPORT || defined(MCXA153)
```

`defined(DRONECAN_SUPPORT)` 检测宏是否存在。`#if DRONECAN_SUPPORT` 检测宏的值。当 `DRONECAN_SUPPORT=0` 时，后者为 false，正确使用 `0x1000`。

### 10.4 最终验证

- PB5 检测（`checkForSignal`）：原版代码正常工作，无需任何修改。外部上拉 + 内部下拉的电压分压使 TEST 1 能越过阈值触发跳转。
- Configurator 连接：正常 ✅
- 主固件跳转：正常 ✅
- 串口遥测：原版代码正常工作 ✅
- Bootloader 串口超时：原版代码正常工作（TEST 3 浮空检测），无需添加超时

### 10.5 修改文件汇总

| 文件 | 修改 |
|------|------|
| `bootloader/main.c:65` | `#if defined(DRONECAN_SUPPORT)` → `#if DRONECAN_SUPPORT` |

---

## 十一、低速抖动根因定位与过零基准修复（2026-07-24）

> 本章由当日独立诊断文档《2026-07-24-低速抖动诊断分析》合并而来，记录一次决定性的根因定位与最小改动修复。

### 11.1 症状

- AM32 Configurator 经 PB5(DShot) 能连接、有遥测回传，Arm 后自检音正常。
- **只有关闭堵转保护(stall_protection)** 电机才转，且转速极低(约 150 rpm)、剧烈抖动，遥测 RPM 也抖。

### 11.2 万用表阻抗矩阵逆向（决定性证据）

电机三相断开、无源，实测各 ADC 引脚到三路电机输出(A/B/C)及 GND 的阻值(kOhm)：

| 引脚 | ->A | ->B | ->C | ->GND |
|---|---|---|---|---|
| PA0 | **5.1** | 7.1 | 7.1 | 1.0 |
| PA1 | 7.1 | **5.1** | 7.1 | 1.0 |
| PA2 | 7.1 | 7.1 | **5.1** | 1.0 |
| PA5 | 7.1 | 7.1 | 7.1 | 1.0 |

节点法反推：每相 `相X --5.1K--> PAi --1.0K--> GND`，三条链路彼此独立；到"他相"的 7.1K = 1.0 + 1.0 + 5.1，是纯经 GND 的间接路径。

结论：
- **相位映射正确**：PA0=相A、PA1=相B、PA2=相C（5.1K 直连一一对应），排除 BEMF 通道接反。
- **本板没有虚拟中性点**：若 PA5 是中性点，PA5->相应 < 7.1K；实测恰好都是 7.1K（纯间接），证明 PA5 未接任何相，**只带一只 1.0K 下拉**。

### 11.3 PA5 下拉的用途分析

- 电气上这只 1.0K 下拉当前唯一作用：把 PA5 钳在 ~0V，使空脚从"随机噪声"变成"稳定的假基准"——这解释了症状为何是"稳定地开环强拖"而非完全乱抖。
- 下拉值 1.0K 与 PA0/1/2 分压网络下臂完全同族，说明 PA5 在版图上是按"第 4 路 BEMF 式分压输入"预留的兄弟脚，其上臂（到某信号源的 5.1K）在本板未安装。
- PA5 在 G431 上同时是 DAC1_OUT2 与 COMP2 负端 IO1。是否原厂曾用 DAC 主动输出基准，存疑（若如此更省事的做法是片内直接路由 DAC 到比较器负端，不必引脚+重载），**倾向"无源第 4 路、上臂未装"**，但不排除 DAC 用途。判别法：跑原厂固件时用示波器量 PA5 是否为随母线变化的稳定直流（是→DAC 主动基准；恒 0V→钳零废脚）。

### 11.4 根本原因

现役 `getCompOutputLevel()` 把过零基准取自 `readADC_ZCD(NEUTRAL_ADC_CHANNEL)`(PA5)——一个被 1K 钳在 ~0V 的空脚。拿 ≈0 的噪声当过零基准，闭环从原理上就建立不起来，`zero_crosses` 涨不上去 → 堵转保护立即触发 → 只有关掉它才能靠开环强拖出 ~150rpm 抖动运行。这比"两次异步采样时序"更本质：**基准脚是空的，采样时序再完美也测不出真过零**。

### 11.5 修复（路线 B：最小改动，仅改基准）

**改动文件**：`Mcu/g431/Src/comparator.c`，仅 `getCompOutputLevel()` 一个函数（`USE_ADC_ZCD` 分支）。

**方案**：软件重构虚拟中性点 = (Va+Vb+Vc)/3。
- 悬空相在真正过零瞬间恰等于三相分压读数的平均；
- 三相分压比实测完全一致(5.1K/1.0K)，故该基准**不依赖分压比、也不依赖母线电压读数**，天然自洽；
- 不再读 PA5(`NEUTRAL_ADC_CHANNEL` 就此弃用，宏保留不删以免影响编译)。

```c
uint8_t getCompOutputLevel(void)
{
    uint16_t va = readADC_ZCD(BEMF_A_ADC_CHANNEL);
    uint16_t vb = readADC_ZCD(BEMF_B_ADC_CHANNEL);
    uint16_t vc = readADC_ZCD(BEMF_C_ADC_CHANNEL);
    uint16_t neutral = (uint16_t)(((uint32_t)va + vb + vc) / 3u);
    uint16_t bemf;
    switch (adc_zcd_phase) {
        case 1: case 4: bemf = vc; break;  /* C floating */
        case 2: case 5: bemf = va; break;  /* A floating */
        case 3: case 6: bemf = vb; break;  /* B floating */
        default:        bemf = va; break;
    }
    return (bemf > neutral) ? 1u : 0u;
}
```

**已否决的错误方案**（记录避免重犯）：
- 固定基准 2048 是错的：BEMF 分压比 1/6.1，3S 过零电压 Vbus/2=6.3V 分压后仅 ~1.03V → ADC≈1280，用 2048 会使 `bemf>neutral` 几乎恒假。
- "母线/2 换算"依赖 `TARGET_VOLTAGE_DIVIDER`(本板走默认 110、未实测)，基准会偏；(Va+Vb+Vc)/3 无此依赖，更鲁棒。

**已知副作用（诚实记录）**：该函数由读 2 次 ADC 变为读 3 次，单次 `getBemfState()` 慢约 50%。路线 B 仅用于验证"基准正确后能否闭环"；提速留给路线 C。

### 11.6 采样率可行性核算（14 磁极，目标 4000~6000 rpm）

- 7 对极，电频率 = rpm × 7 / 60。4000rpm→466.7Hz，6000rpm→700Hz；每步(过零间隔) 4000rpm≈357μs、6000rpm≈238μs。
- ADC 单通道转换(170MHz/DIV4)≈0.6μs，采三相≈1.8μs，远非瓶颈。
- 同步采样率 = PWM 载波频率(地面站 `pwm_frequency`，合法 8~48kHz；24k 只是标称默认值 `NOMINAL_PWM`)。

| 地面站 PWM | 6000rpm 每步采样点数 | 评价 |
|---|---|---|
| 24 kHz | ≈5.7 点/步 | 够用，偏紧 |
| **48 kHz** | **≈11.4 点/步** | **充足，推荐** |

**结论：跟得上。** 建议地面站将 PWM 频率设为 **48kHz**（纯配置项，零代码、零硬件改动）以获得充足余量。

### 11.7 后续（路线 C，验证 B 通过后再议）

- 用 TIM1 触发 ADC 注入组，在 PWM ON 窗口内同步采 BEMF+三相，消除阻塞异步采样；
- 换相后加 blanking 跳过续流期；
- 视效果决定是否还原换相相关的 `old_routine` 锁定（隐患3/隐患4）。
- 若愿飞线把相 C 的 BEMF 从 PA2 挪到空闲的 PA7(COMP2 正端)，可整体切硬件比较器 + DAC/VREFINT 门限，最省 CPU、最接近原厂。

### 11.8 验证步骤

1. 地面站将 PWM 频率设为 48kHz。
2. 重新编译主固件、烧录。
3. **恢复开启堵转保护**，给油门：若电机能平稳起转、`zero_crosses` 正常增长、遥测 RPM 稳定，则根因判断与修复成立。
4. 若仍抖，转入路线 C。

---

## 十二、路线 B 无效后的现场诊断（GDB 实时抓取三相 ADC）

> 背景：路线 B（软件重构 `(Va+Vb+Vc)/3` 基准）落地后，现象**没有变化**——低油门仍只能靠开环强拖缓慢抖动、`stall_protection` 一开就保护。`min_bemf_counts` 从 30 改回 3 也无效。另一强线索：**只有打开互补 PWM（comp_pwm）时** 10% 以下油门电机才能缓慢抖转，关掉则只抖两下。
>
> 这基本坐实：**当前电机是"开环强拖"在转，BEMF 闭环从未建立**（互补 PWM 在低占空比下用低边同步整流增大力矩，让开环强拖更有劲，与过零检测无关）。继续盲改代码没有意义，**必须先拿到运行时真实的三相 ADC 读数与 `zero_crosses` 走势**，用数据区分三种根因。
>
> 为此在 `Mcu/g431/Src/comparator.c` 增加了一组 `volatile` 文件级诊断变量（**只镜像内部读数、不改变任何控制逻辑**），可用 SWD + GDB 实时观测。**本章供 Mac 端调试环境（OpenOCD + arm-none-eabi-gdb）直接执行。**

### 12.1 已加入的诊断变量（comparator.c，`USE_ADC_ZCD` 分支）

```c
volatile uint16_t zcd_dbg_va;       /* 最近一次相 A 分压 ADC 读数 */
volatile uint16_t zcd_dbg_vb;       /* 相 B */
volatile uint16_t zcd_dbg_vc;       /* 相 C */
volatile uint16_t zcd_dbg_neutral;  /* 软件中性点 = (va+vb+vc)/3 */
volatile uint16_t zcd_dbg_bemf;     /* 本次选中的悬空相读数 */
volatile uint8_t  zcd_dbg_out;      /* 最近一次极性判决 (1 = bemf>neutral) */
volatile uint8_t  zcd_dbg_phase;    /* 当前 adc_zcd_phase (1..6) */
volatile uint32_t zcd_dbg_calls;    /* getCompOutputLevel() 累计调用次数 */
volatile uint32_t zcd_dbg_flips;    /* 极性翻转累计次数 */
```

这些变量在 `getCompOutputLevel()` 末尾被写入，`volatile` 保证不被优化掉、GDB 能稳定读取。调试完成后可整段删除。



### 12.3 重新编译并烧录（务必确认新固件真的在跑）

```bash
cd <AM32-main 仓库根目录>
make TARGET=CUSTOM_FD6288_G431 ARM_SDK_PREFIX=<arm-none-eabi- 前缀路径>

# 反汇编自检：getCompOutputLevel 内应能看到三次 readADC_ZCD 调用，
# 且能看到写 zcd_dbg_* 的存储指令；否则说明烧进去的是旧固件。
arm-none-eabi-objdump -d obj/AM32_CUSTOM_FD6288_G431_2.20.elf \
  | grep -A60 '<getCompOutputLevel>:' | grep -E 'readADC_ZCD|zcd_dbg'
```

烧录（SWD，写主固件到 `0x08001000`，勿覆盖 bootloader）：

```bash
openocd -f interface/stlink.cfg -f target/stm32g4x.cfg \
  -c "program obj/AM32_CUSTOM_FD6288_G431_2.20.bin 0x08001000 verify reset exit"
```

### 12.4 GDB 实时观测

```bash
# 终端 1：启动 OpenOCD（保持运行）
openocd -f interface/stlink.cfg -f target/stm32g4x.cfg

# 终端 2：GDB attach（不要 reset，attach 到正在运行的固件）
arm-none-eabi-gdb obj/AM32_CUSTOM_FD6288_G431_2.20.elf
(gdb) target remote :3333
(gdb) set pagination off
```

**关键前提：地面站恢复到干净的 sensorless 基线** —— 关闭"霍尔传感器"、自动进角先用 AM32 默认，一次只动一个变量。给一个刚好能让电机抖转的油门（建议先开互补 PWM 以便有稳定现象），然后在 GDB 里反复采样：

```gdb
# 单次快照
(gdb) printf "calls=%u flips=%u zc=%d st=%d ph=%d va=%d vb=%d vc=%d n=%d bemf=%d out=%d\n", \
      zcd_dbg_calls, zcd_dbg_flips, zero_crosses, step, zcd_dbg_phase, \
      zcd_dbg_va, zcd_dbg_vb, zcd_dbg_vc, zcd_dbg_neutral, zcd_dbg_bemf, zcd_dbg_out
```

连续抓一串（每次回车打一行，抓 15~20 行覆盖电机转几圈）：

```gdb
define zsnap
  printf "zc=%d st=%d ph=%d va=%d vb=%d vc=%d n=%d bemf=%d out=%d flips=%u\n", \
    zero_crosses, step, zcd_dbg_phase, zcd_dbg_va, zcd_dbg_vb, zcd_dbg_vc, \
    zcd_dbg_neutral, zcd_dbg_bemf, zcd_dbg_out, zcd_dbg_flips
end
# 反复敲 zsnap 采样；或用 GDB 的连续读：
(gdb) zsnap
(gdb) zsnap
...
```

> 注意：OpenOCD/GDB 读全局变量属**非侵入内存读**，无需 halt CPU，电机可保持运行。若某些配置下读值需要 halt，则改用 `monitor mdw <addr> N` 直接读内存地址（用 `(gdb) print &zcd_dbg_va` 得到地址）。

### 12.5 判读表（把抓到的 15~20 行对照本表）

| 现象（观测数据特征） | 根因诊断 | 下一步对策 |
|---|---|---|
| `zero_crosses` 长期为 0 或个位数，且 `va/vb/vc` 三者几乎相等（差 < 50）、几乎不随 `st` 变化 | **PWM 开关噪声淹没 BEMF / 异步采样恰好落在续流期**：三相都是"脏值"，平均后 `bemf≈neutral`，永远比不出真过零 | 路线 B 原理上救不活，**必须上路线 C**：TIM1 触发 ADC 注入组做 PWM 同步采样 + 换相后 blanking |
| `va/vb/vc` 有明显差异（悬空相能看出梯形波起伏），但 `zero_crosses` 一直不涨、`flips` 却很多（乱翻） | 采样点分散在整个 PWM 周期，脏点导致极性乱跳 | 同样需要 PWM 同步采样 + blanking（路线 C） |
| `va/vb/vc` 差异明显、`flips` 合理，但 `zero_crosses` 始终 0、`out` 恒为 1 或恒为 0 | **极性/相序判反**：过零方向被判死 | 交换悬空相映射（case 1/4 与 2/5 对调），或把判据 `bemf>neutral` 改为 `<`；单行改动 |
| `zero_crosses` 能涨到几十~几百后又清零、反复循环 | 起转期能抓到过零，但保持不住闭环（采样落续流期造成假过零累计触发失步） | 加换相后 blanking（路线 C 的一部分） |
| `zcd_dbg_calls` 根本不增长 | `getCompOutputLevel()` 没被调用 → 系统不在 `old_routine=1` 轮询路径 | 回查 main.c 启动路径与 `old_routine` 锁定（隐患3/隐患4），确认轮询分支在跑 |

### 12.6 互补 PWM 线索的解读（已确认信息）

- comp_pwm 开 → 低油门能缓慢抖转；关 → 只抖两下。**这是"开环强拖"的典型特征**，不是闭环工作的证据。
- 因此在拿到 12.5 的数据前，**不要**把"comp_pwm 打开后能转"误判为"修复见效"。真正的成功判据只有一个：**`zero_crosses` 能持续稳定增长**。

### 12.7 备用方案：不接 SWD 时用串口打印

若 Mac 侧 SWD 不便，可在 `getCompOutputLevel()` 末尾临时加限频 printf（走已修好的 PB6/USART1 遥测串口，115200-8N1）：

```c
static uint32_t _n = 0;
if ((++_n & 0xFF) == 0) {   /* 每 256 次打一行，避免打爆串口 */
    printf("zc=%ld st=%u va=%u vb=%u vc=%u n=%u bemf=%u out=%u\r\n",
           (long)zero_crosses, adc_zcd_phase, va, vb, vc, neutral, bemf, out);
}
```
（需 `extern` 引入 `zero_crosses`，并确保 printf 已重定向到 USART1。）抓到的行同样按 12.5 判读。

### 12.8 2026-07-25 现场诊断实测数据

#### 12.8.1 测试条件

- 电机：1000KV、2S 电池、8V 供电
- `min_bemf_counts=3`，`stall_protection=关闭`
- 互补 PWM（`comp_pwm`=打开）下电机能缓慢抖动（~150 RPM 开环强拖）
- 测试工具：OpenOCD 0.12.0 + `arm-none-eabi-gdb`，非侵入式内存读（`monitor mdw/mdh`，不 halt CPU）
- **必须 `monitor resume` 先恢复 CPU 运行**再读内存，否则数据完全冻结

#### 12.8.2 原始数据

注：`mdh` 读 5 个半字不能保证原子性，单行内 va/vb/vc/neutral/bemf 可能来自不同的 `getCompOutputLevel()` 调用。

```
SNAP0: va=1   vb=0  vc=0   neutral=1    bemf=0   step=5  zc=2  calls=793k
SNAP1: va=2   vb=0  vc=4   neutral=543  bemf=68  step=4  zc=1  calls=803k
SNAP2: va=0   vb=2  vc=0   neutral=1    bemf=0   step=2  zc=2  calls=812k
SNAP3: va=3   vb=0  vc=0   neutral=400  bemf=0   step=4  zc=1  calls=821k
SNAP4: va=0   vb=2  vc=0   neutral=176  bemf=0   step=2  zc=2  calls=835k
```

#### 12.8.3 判读

| 观测 | 结论 |
|------|------|
| `calls` 以 ~16,800 次/秒增长 | ✅ `getCompOutputLevel()` 在 `old_routine=1` 路径上正常运行 |
| `step` 持续变化 (2→4→5→4→2) | ✅ COM_TIMER 开环换相在工作 |
| `va/vb/vc` 绝大多数为 0~4 counts | ⚠️ ADC 读数极低，BEMF 信号极其微弱 |
| `neutral` 偶尔跳高到 176~543 | ⚠️ 采样偶尔落在 PWM ON 窗口能读到有效电压，但极不稳定 |
| `zero_crosses` 始终卡在 1~2 | ❌ 过零检测永远建立不了闭环 |

#### 12.8.4 根因：起点转速太低，BEMF 不可检测

1000KV 电机在 2S（8V）下空载转速 8,000 RPM。150 RPM 开环强拖时：

- BEMF = 150/8000 × 8V = **150 mV**（相电压）
- 经 6.1:1 分压后到 ADC = **24.6 mV**
- 12-bit ADC 读数 ≈ **30 counts**

而实测读数仅 0~4 counts（等效 < 3.2 mV），意味着实际转速可能远低于 150 RPM。

**BEMF 信号在此转速下与噪声处于同一量级**，无论中性点算法多精确（路线 B），信噪比不足就无法稳定检测过零——这是基本原理限制，不是软件能绕过的。

#### 12.8.5 方向判断

路线 B（软件 `(Va+Vb+Vc)/3` 中性点）原理正确，但当前问题的瓶颈**不是中性点精度**，而是：

1. **BEMF 信号绝对值太小**（< 10 mV，仅 ~11 ADC counts）
2. **采样时机与 PWM 不同步**，大部分落在 OFF 期/续流期，读到 0
3. 偶尔落在 ON 窗口时（neutral=400~543）说明 ADC 本身工作正常，但随机采样无法稳定形成过零序列

下一步需 **路线 C**：用 TIM1 触发 ADC 注入组在 PWM ON 窗口内同步采样 + 换相后 blanking。这是提高低速 BEMF 检测可靠性的必然路径。

### 12.9 复核意见（G431 主机侧 AI，2026-07-25）

> 说明：本节是对 12.8 的**复核与修正**，不改动 12.8 原文。核对范围：AM32 原版 `Src/main.c` 启动/换相路径、`Mcu/g431/Src/ADC.c` 的 `readADC_ZCD()` 采样方式、`Mcu/g431/Src/comparator.c` 诊断代码。备份见 `.backup/20260725-153755/`。

**结论先行**：路线 C（PWM 同步采样 + 换相后 blanking）方向正确、迟早要做；但 12.8"BEMF 信号绝对太小、是物理原理限制"的表述**不准确**，且 12.8.2 的原始数据不足以定量支撑该结论。真正主因更可能是"异步采样落续流窗口"叠加"开环强拖可能失步"，需再补一次时序观测坐实。

#### 12.9.1 更正一：AM32 没有传统盲换相 ramp，隐患4 与原版等价

原版 `startMotor()`（main.c 约 938 行）本身就是 `commutate() + commutation_interval=10000 + SET_INTERVAL_TIMER_COUNT(5000)`，与隐患4 内联改动等价。AM32 的"起转加速"实际靠两处：

- `PeriodElapsedCallback()`（COM_TIMER 中断）：在 `zero_crosses < 50` 期间反复强制换相，且
  `commutation_interval = (commutation_interval + (lastzctime + thiszctime) >> 1) >> 1`。
  起转期 `lastzctime/thiszctime = 0`，所以 interval **每次折半**（10000→5000→2500→1250→…），是几何塌缩式开环强拖，不是固定步进表。
- `zcfoundroutine()`（轮询路径抓到真实过零后）：用实测过零时间刷新 `commutation_interval`，闭环接管并把转速拉起来。

因此"起转 ramp 被改动破坏"不成立，不要再往这个方向改。

#### 12.9.2 更正二：12.8.2 原始数据时间上不连贯，不能按行解读

`mdh` 非原子（文档已注），且仅 5 行、每行间隔约 1 万次调用（793k→803k→…），属稀疏快照。用算术即可验证同一行的 `neutral` 与 `va/vb/vc` 来自不同调用：

- SNAP1：`(va+vb+vc)/3 = (2+0+4)/3 = 2`，不可能等于同行 `neutral=543`；
- SNAP3：`(3+0+0)/3 = 1`，不可能等于同行 `neutral=400`。

所以**这组 raw 数据不能按行做定量推断**（BEMF 幅度、转速反推均不可靠）。若要定量，必须改用 GDB 的 `zsnap` 宏在**同一次**函数快照里一次性打印 va/vb/vc/neutral/bemf（12.4 已给），而不是分别 `mdh`。

#### 12.9.3 更正三："信号太小"应改为"采样时机未与 PWM 同步"

恰恰是 12.8.2 里 `neutral` 能跳到 400~543 counts 这一点，反证了 **ADC + 6.1:1 分压在某些采样点能读到 0.3~0.5V 有效电压**。因此瓶颈不是"ADC 读不到 / 幅度到了物理极限"，而是：

- `readADC_ZCD()` 是**阻塞单次转换、与 PWM 载波异步**（见 ADC.c 注释也已说明）；
- 绝大多数采样落在 PWM OFF / 续流窗口 → 读到 0~4 counts；
- 偶尔落在 ON 窗口 → 读到几百 counts（即那几个 400~543）。

这解释了 va/vb/vc 大多为 0 而 neutral 偶尔跳高的现象，**是采样时机问题，不是幅度物理极限**。路线 C 的价值正在于把采样锁进 ON 窗口中点，让每次读数都有效。

#### 12.9.4 被 12.8 忽略的并列嫌疑：开环强拖可能失步（zc 卡 1~2 的疑点）

若 `PeriodElapsedCallback` 在持续强拖，`zero_crosses` 会因每次强制换相都自增（main.c 约 902 行 `if (zero_crosses < 10000) zero_crosses++;`）而**很快冲到 50 才停**。实测却长期卡在 1~2，提示强拖**未持续或被 desync 重启打断**：interval 每步折半，十几步内塌到近 0，电机跟不上 → 失步抖动 → 可能触发 desync/重启，`running` 复位、`zero_crosses` 清零，再从 10000 起，形成"缓慢抖转"的死循环。这与 comp_pwm 打开才能抖转（低边同步整流给强拖更多力矩）完全自洽。

#### 12.9.5 动大代码前建议先补一次时序观测（5 分钟，零代码改动）

用现有诊断变量 + GDB 连续读，重点看**时间序列**而非单点：

```gdb
define tsnap
  printf "t running=%d zc=%d st=%d ci=%d va=%d vb=%d vc=%d n=%d bemf=%d flips=%u\n", \
    running, zero_crosses, step, commutation_interval, \
    zcd_dbg_va, zcd_dbg_vb, zcd_dbg_vc, zcd_dbg_neutral, zcd_dbg_bemf, zcd_dbg_flips
end
# 快速连打 30~50 次，或 while 循环自动采样
```

判读（补充 12.5）：

| 时序特征 | 根因 | 对策 |
|---|---|---|
| `commutation_interval` 反复从 ~10000 骤降到近 0、`running` 或 `zero_crosses` 周期性清零 | 开环强拖塌缩过快 → 失步 → desync 重启循环 | 先让起转 ramp 更平缓（限制 interval 下限 / 减慢折半），把电机拖到能出 BEMF 的转速；再谈路线 C |
| `commutation_interval` 稳定在某值、`va/vb/vc` 单次快照里悬空相明显区别于另两相、但 `flips` 乱跳 | 采样落续流、极性乱翻 | 路线 C：PWM 同步采样 + 换相后 blanking |
| 同上但 `bemf` 与 `neutral` 始终同号 | 极性/相序判反 | 交换悬空相映射或反转判据（单行改动，见 12.5） |

#### 12.9.6 路线 C 最小实现要点（参考 AM32 官方做法）

AM32 官方在硬件比较器路径本就用两个手段规避 PWM 噪声，移植到 ADC 时对应如下（保持最小改动）：

1. **换相后 blanking**：官方 `waitTime = commutation_interval/2 - advance` 本身就跳过换相后前半个电周期再开始找过零。ADC 版应在 `changeCompInput()` 后跳过前 N 次采样，避免读到换相尖峰/续流。
2. **多次采样滤波**：官方 `interruptRoutine()` 用 `filter_level` 连续确认。ADC 版可在 `getCompOutputLevel()` 内对悬空相多点采样取多数表决。
3. **PWM 同步**：把 `readADC_ZCD` 从"主循环随机阻塞采样"改为"TIM1 更新/CC 事件触发 ADC 注入组，在 ON 窗口中点采样悬空相 + 母线"。这是路线 C 的核心，但**改动量最大，务必先完成 12.9.5 的时序确认再动手**，以免误判方向。

> 一句话给测试同事：**成功判据仍只有一个——`zero_crosses` 能持续稳定增长**（12.6）。在此之前的一切"能抖转"都可能是开环强拖假象。

### 12.10 时序数据实测判定（310 行原子采样，2026-07-25）

> 数据源：`AM32-FD6288-G431-5min-capture.log`，由固件内部环形缓冲一次性 dump 的 310 条**原子完整行**（同一次 `getCompOutputLevel()` 内写入，不存在 12.8 的跨调用拼凑问题），字段：`idx va vb vc neutral bemf zc ci step running out phase`。这份数据同时**证伪了 12.9.4 的失步假设**，并把根因锁死为"采样未与 PWM 同步"。

#### 12.10.1 全样本统计

| 指标 | 实测 | 含义 |
|---|---|---|
| `running` | 恒 = 1（310/310） | **无 desync 重启循环** → 证伪 12.9.4 的"重启死循环"假设 |
| `commutation_interval` (ci) | 23075~41203，均值 **28720** | **没有折半塌缩到 0** → 证伪"interval 塌陷失步" |
| `zero_crosses` (zc) | 只在 1↔2 抖动（1:123 次, 2:187 次） | 闭环从未建立，偶发假过零反复触发又不连续 |
| 悬空相 `bemf > 100` | 仅 **8.1%**（25/310） | **92% 的悬空相采样读到 0** |
| 三相任一 `> 100` | 仅 **14.8%**（46/310） | **约 85% 采样落在 PWM OFF / 续流窗口** |
| 有效点大值落"悬空相 : 驱动相" | 25 : 21（近乎各半） | 有效采样点极性**无规律** → 采样时机随机 |

#### 12.10.2 转速换算（关键）

`INTERVAL_TIMER` 的 `Prescaler = PCLK_MHZ - 1` → 定时器 tick = 1 MHz，即 **1 count = 1 us**。

- ci 均值 28720 counts = **28.7 ms / 换相步**；
- 一个电周期 = 6 步 = 172 ms → 电频率 ≈ **5.8 Hz**；
- 14 磁极 = 7 对极 → 机械转速 = 5.8 / 7 × 60 ≈ **50 rpm**。

即电机实际只有 ~50 rpm 的开环强拖，比 12.8 估的 150 rpm 还低。

#### 12.10.3 完整因果链（确定性，取代 12.8.4 的"物理极限"表述）

```
低速 + comp_pwm 开（续流时低管导通把悬空相钳到 ~0V）
   → 约 85% BEMF 采样落在 OFF/续流窗口，读到 0（实测悬空相 92% 读 0）
   → 极少数落入 ON 窗口的读数（400~1650 counts）夹杂噪声，偶发触发"假过零"
   → zc 在 1↔2 抖动，永远到不了闭环所需的连续增长
   → 假过零经 zcfoundroutine 的 ci = (thiszctime + 3*ci)/4 把换相间隔越拉越大
     （初值 10000 被拉到均值 28720）
   → 换相更慢 → 转速掉到 ~50 rpm → BEMF 更小 → 永远无法闭环
```

这条链自洽解释了全部现象：只能开环强拖、缓慢抖动、zc 不涨、ci 偏大且抖动、comp_pwm 开才有劲。

#### 12.10.4 三项根因逐一裁决

| 候选根因 | 数据裁决 | 结论 |
|---|---|---|
| 起转 ramp 塌陷失步（12.9.4） | ci 稳定在 ~28ms 不塌缩、running 恒 1 | **否决** |
| 极性 / 相序判反（12.5 第 3 行） | 有效点大值落悬空相:驱动相 = 25:21，无固定偏向；out=1 占 25% 也无规律 | **否决**（不是判死，是随机） |
| 采样未与 PWM 同步、落续流窗口 | 85% 采样读 0，仅偶发 ON 窗口有效 | **确认，唯一主因** |

#### 12.10.5 方向定论与下一步

**路线 C 是唯一正解，现在有硬数据支撑，不再是猜测。** 按 12.9.6 的最小实现顺序推进：

1. **第一步（改动最小、先验证）——换相后 blanking + 有效性门控**：在 `getCompOutputLevel()` 里，若三相读数全 < 阈值（如 100 counts）判为"无效采样"直接返回上次结果，不参与过零计数；并在 `changeCompInput()` 后跳过前 N 次采样。目的：先把 92% 的续流期脏 0 挡在过零逻辑之外，看 zc 是否开始增长。此步不动 ADC 触发架构，风险最低。
2. **第二步（核心）——PWM 同步采样**：用 TIM1 更新/CC4 事件触发 ADC 注入组，在 PWM ON 窗口中点采样悬空相 + 母线，彻底消除阻塞异步采样。
3. **可选第三步**——多点采样多数表决，进一步抗噪。

> 提示：若第一步（blanking + 门控，约十几行、单文件 `comparator.c`）就能让 zc 稳定增长，则可能无需动 TIM1/ADC 架构，符合最小改动原则。建议先做第一步再评估是否必须上第二步。


### 12.11 业界做法调研 + 路线 C（PWM 同步采样）落地设计（2026-07-25）

> 用户决策：倾向直接上 PWM 同步采样，并指出 G431 资源充裕。本节先给出对主流开源/厂商实现的调研结论，再据此给出本板 G431 的具体落地方案。所有结论均可回溯到源码位置或文档编号。

#### 12.11.1 为什么必须"在 PWM 高电平(ON)、且远离开关沿"采样

6-step 梯形驱动下三相只有两相通电，第三相悬空，BEMF 就测在悬空相上。但 PWM 在斩波：

- **PWM ON（驱动 FET 导通）**：悬空相通过绕组耦合到母线，电压 = 真实反电动势 + 一个确定直流偏置，波形干净，可与虚拟中性点比较。
- **PWM OFF（续流 / damped）**：电流经续流二极管或同步整流 FET 续流，悬空相被钳到 ~0V 或 Vbus，BEMF 被完全淹没 —— 采到的是无意义的续流电平（这正是本板实测 92% 悬空相读 0 的物理原因，见 12.10）。
- **开关沿附近**：dv/dt + 寄生电感产生几百 ns 到几 us 的振铃与尖峰，会造成误判。

结论：异步盲采样（本板现状）会随机落在 OFF 段或开关沿上，得到近乎随机的过零判定 —— 这与 12.10 的数据完全吻合。

#### 12.11.2 主流实现怎么做（可回溯来源）

| 实现 | 采样时机机制 | 消隐(blanking) | 中性点 | 来源 |
|---|---|---|---|---|
| **VESC** (Benjamin Vedder) | `val_sample = duty/2`（ON 脉冲中点），写入 `TIM8->CCR1`，由定时器比较事件在每个 PWM 周期硬件触发 ADC | 有 | 可用真实/虚拟中性点 | `motor/mcpwm.c`（`val_sample=duty/2` 约 2370 行；`TIM8->CCR1=val_sample` 约 2665 行）https://github.com/vedderb/bldc/blob/master/motor/mcpwm.c |
| **AM32 官方 (g071)** | 硬件比较器 + **TIM1 OC5 作为 comparator blanking source**，比较器在开关噪声窗口内忽略输入 | 有（TIM1 CH5/OC5） | 硬件比较器 IO 比较 | `Mcu/g071/Src/comparator.c` + `peripherals.c`（`LL_TIM_CHANNEL_CH5 ... // timer used for comparator blanking`，约 742 行）；版本历史 "1.80 Enable Comparator blanking for g071 on timer 1 channel 4"、"1.92 Move g071 comparator blanking to TIM1 OC5" |
| **ST 官方 6-step 例程 / AN** | TIM1 CC4 或 update 事件触发 ADC 注入组，在 PWM ON 中点采 BEMF | 换相后消隐若干周期 | 可用虚拟中性点 | STM32 sensorless BLDC 6-step 例程 / AN 系列 |
| **TI trapezoidal** | PWM 中点 ADC 同步采样 + 换相 blanking | 有 | 虚拟中性点 | TI sensorless 梯形控制 app note |

**共性提炼（三条铁律）**：

1. **ON 中点采样**：采样点设在 ON 脉冲正中（距上升沿/下降沿各 ~duty/2），振铃衰减最干净、信噪比最高，且随占空比自动缩放。
2. **换相后 blanking**：换相瞬间续流尖峰会误触发，需消隐一段（典型几个 PWM 周期，或换相间隔的前 ~1/8 ~ 1/4 电角度）后再启用过零判定。AM32/getBemfState 已有的 `filter_level`（连续 N 次一致才认账）也是一种时间维度的滤波。
3. **软件虚拟中性点足够**：无需真实中性点引脚，`(Va+Vb+Vc)/3` 即可（本板 12.11 路线 B 已实现），VESC/ST 例程亦如此。

#### 12.11.3 本板 G431 资源核实（决定方案可行性）

| 资源 | 现状（已核实） | 对方案的影响 |
|---|---|---|
| TIM1 | `CounterMode = UP`（**边沿对齐**，非中心对齐），`Prescaler=0`，`Autoreload=TIM1_AUTORELOAD`；G431 段 CPU/PWM 时基下 ARR≈6665（160MHz/24kHz），CH1/2/3 三相 PWM，CH4/CH5 空闲 | 边沿对齐时 ON 中点 ≈ `CCRx/2`（高侧计数区）。**CH4 空闲可直接用作 ADC 触发比较通道** |
| ADC | 目前用 **regular 组 + DMA 循环**（`enableADC_DMA`，采温度/电压/电流），`readADC_ZCD()` 用的是**阻塞单次 regular 转换**（会停 DMA 再恢复，异步于 PWM，见 ADC.c:492） | **注入组(injected group) 完全空闲** → 可专给 BEMF 同步采样用，不干扰现有 regular+DMA 链路 |
| 注入组触发 | G431 ADC 注入组支持 `JEXTSEL` 外部触发，可选 TIM1_CH4/TIM1_TRGO 等 | 可实现"TIM1 CH4 → 触发 ADC 注入组转换悬空相"的纯硬件同步 |
| DMA 通道 | DMA1_CH2/CH4 已用于 regular；注入组可用中断读取或独立 DMA | 资源够用 |

结论：**G431 资源充足，路线 C（第二步 PWM 同步采样）技术上完全可行且不与现有 regular ADC/DMA 冲突。**

#### 12.11.4 路线 C 落地架构（第二步，核心）

```
TIM1 (24kHz PWM, 边沿对齐, ARR≈6665)
  ├── CH1/2/3  → 三相互补 PWM（现状不动）
  └── CH4 (OC4) → 比较值设为 ON 中点 ≈ 当前有效占空比 CCR/2
                    │  (作为 ADC 注入组的外部触发 JEXTSEL = TIM1_CH4)
                    ▼
             ADC1 注入组 (injected)
                    │  在每个 PWM 周期 ON 中点被硬件触发，转换"当前悬空相"通道
                    │  (悬空相由 changeCompInput() 依 step 选择注入序列)
                    ▼
             JEOC 中断 / 注入数据寄存器 JDRx
                    │  读到干净的悬空相 BEMF；neutral 用 (Va+Vb+Vc)/3 或母线/2
                    ▼
             过零判定 (bemf ? neutral) → 喂给 getBemfState()/interruptRoutine 的既有闭环
```

要点：
- **触发源**：`CH4` 比较值 = 有效占空比的一半（跟随 duty 自动缩放，命中 ON 中点）。占空比很低时 ON 窗口过窄，退化到"启动阶段特殊处理"（见 12.11.6）。
- **只动采样时机，不动闭环算法**：`zcfoundroutine`/`commutate`/`PeriodElapsedCallback` 全部保持 AM32 原版，只把"脏的阻塞异步读"换成"干净的 PWM 同步注入读"。符合最小改动原则。
- **消隐**：换相后前 N 个 PWM 周期的注入结果不参与过零（配合既有 `filter_level` 连续一致判定）。

#### 12.11.5 分步实施建议（保持最小改动、可回退）

- **第一步（先做，单文件 comparator.c，十几行）**：blanking + 有效性门控（见 12.10.5 第 1 条）。哪怕最终要上第二步，这一步也是低风险的即时验证手段，先看 `zc` 是否开始增长。
- **第二步（核心，路线 C）**：新增 TIM1 CH4 触发 + ADC 注入组配置（peripherals.c / ADC.c），改写 `readADC_ZCD()` 或新增 `readADC_ZCD_injected()`，`getCompOutputLevel()` 改读注入结果。改动集中在 g431 的 ADC/peripherals + comparator，不碰 main.c 闭环。
- **第三步（可选）**：ON 窗口内多点采样 + 多数表决，进一步抗噪。

#### 12.11.6 低速启动阶段的特殊处理（信号弱时）

- 启动占空比低 → ON 窗口窄 → 中点采样窗口也窄；此时 BEMF 本身也小。主流做法是启动段先用**开环强拖 + 逐步提高占空比/降低换相间隔**，等 `zero_crosses` 累积到阈值再切换到 BEMF 闭环 —— 这正是 AM32 `PeriodElapsedCallback` 的既有机制，无需改。
- 关键是：一旦采样时机对齐到 ON 中点，即使低速，落在有效窗口的比例也会从现在的 ~15% 提升到接近 100%，`zc` 才可能连续增长、闭环才能建立。

#### 12.11.7 与用户确认的选择

- **选项 A（推荐先做）**：先落第一步（blanking+门控），单文件、可回退、当天验证 `zc` 是否增长；再决定是否上第二步。
- **选项 B（用户当前倾向）**：直接上第二步 PWM 同步采样（改动较大：peripherals.c + ADC.c + comparator.c 三个文件），一步到位。

> 无论 A/B，闭环算法与 main.c 均不改；两者的差别只是"采样时机"实现的深度与改动面。





---

### 12.12 路线 C 实施记录（PWM 同步注入采样，已落地）

> 本节记录 12.11.7 选项 B（一步到位上 PWM 同步采样）的**实际代码改动**。改动全部集中在 g431 的三个外设/驱动文件 + 一个宏头文件，**未触碰 main.c 及 AM32 闭环算法**，符合最小改动原则。所有改动均以 `#ifdef USE_ADC_ZCD` 包裹，关闭该宏即回退到原版硬件比较器路径。

#### 12.12.1 改动总览（4 处文件）

| 文件 | 改动 | 作用 |
|---|---|---|
| `Mcu/g431/Inc/peripherals.h` | `SET_DUTY_CYCLE_ALL` 宏扩展：新增 `TIM1->CCR4 = duty>>1` | 让 CH4 比较值自动跟随占空比，命中 ON 脉冲中点 |
| `Mcu/g431/Src/peripherals.c` | `MX_TIM1_Init()` 增加 CH4 (OC4) 初始化；`enableCorePeripherals()` 使能 CH4 | CH4 作为纯内部 ADC 触发通道（不映射 GPIO） |
| `Mcu/g431/Src/ADC.c` | `ADC_Init()` 增加注入组配置；`activateADC()` 增加 `LL_ADC_INJ_StartConversion()` | 配置 ADC1 注入组由 TIM1_CH4 硬件触发，采 3 相 BEMF |
| `Mcu/g431/Src/comparator.c` | `getCompOutputLevel()` 改读注入结果 + 换相后 blanking；`changeCompInput()` 重装 blanking | 用干净的 PWM 同步注入数据做过零判定 |

#### 12.12.2 关键设计点

1. **触发链路（纯硬件，零 CPU 干预）**
   `TIM1 CH4 (OC4, PWM1, CCR4=duty/2)` → 每个 PWM 周期在 ON 中点产生比较事件 → `JEXTSEL=TIM1_CH4, 上升沿` 触发 ADC1 注入组 → 一次转换 rank1/2/3 = BEMF A/B/C → 结果落入 JDR1/2/3。注入组仅在 `activateADC()` 里 arm 一次（`LL_ADC_INJ_StartConversion`），此后每个 CH4 事件自动重启序列，无需软件再启动。

2. **为什么 ON 中点 = CCR4/2**
   TIM1 为边沿对齐（`COUNTERMODE_UP`），PWM1 模式下高侧在 `CNT < CCRx` 期间导通，故 ON 脉冲区间为 `[0, CCRx)`，其中点为 `CCRx/2`。三相占空比相同（`CCR1=CCR2=CCR3=duty`），令 `CCR4=duty/2` 即命中中点。占空比变化时中点自动跟随。

3. **与现有 regular+DMA 链路完全隔离**
   温度/电压/电流仍走 regular 组 + DMA1_CH2 循环（`LL_ADC_REG_DMA_TRANSFER_LIMITED`，main.c:2028-2031 独立重新 arm）。注入组与 regular 组在 G431 ADC 上是**两套独立的序列器与数据寄存器**，互不干扰，**遥测不受影响**。

4. **软件虚拟中性点**
   本板无物理中性点引脚（PA5 仅 1.0k 下拉，未接任何相）。`getCompOutputLevel()` 内部用 `neutral = (Va+Vb+Vc)/3` 重构；三个分压电阻一致（5.1k/1.0k），故参考点与分压比、母线电压无关。

5. **换相后消隐（blanking）**
   `changeCompInput()` 每次换相把 `adc_zcd_blank` 重装为 `ADC_ZCD_BLANK_CALLS`（默认 3）。消隐窗口内 `getCompOutputLevel()` 返回上一次稳定极性且**不更新**，避免续流尖峰产生的假过零推进 AM32 的过零计数器。与既有 `TARGET_MIN_BEMF_COUNTS` 连续一致滤波互补。

6. **悬空相选择**（沿用原有换相步映射）
   step 1/4 → C 悬空（读 vc）；step 2/5 → A 悬空（读 va）；step 3/6 → B 悬空（读 vb）。

#### 12.12.3 各文件改动细节

**(a) peripherals.h — SET_DUTY_CYCLE_ALL**
```c
#define SET_DUTY_CYCLE_ALL(newdc) \
    (TIM1->CCR1 = newdc, TIM1->CCR2 = newdc, TIM1->CCR3 = newdc, \
        TIM1->CCR4 = (uint16_t)((newdc) >> 1))
```

**(b) peripherals.c — MX_TIM1_Init() 在 CH3 块之后、SetTriggerOutput 之前新增**
```c
LL_TIM_OC_EnablePreload(TIM1, LL_TIM_CHANNEL_CH4);
TIM_OC_InitStruct.CompareValue = 0;
LL_TIM_OC_Init(TIM1, LL_TIM_CHANNEL_CH4, &TIM_OC_InitStruct);
LL_TIM_OC_DisableFast(TIM1, LL_TIM_CHANNEL_CH4);
```
**peripherals.c — enableCorePeripherals() 在 CH3N 之后新增**
```c
#ifdef USE_ADC_ZCD
    LL_TIM_CC_EnableChannel(TIM1, LL_TIM_CHANNEL_CH4);
#endif
```

**(c) ADC.c — ADC_Init() 非 USE_ADC_1_2 路径，电流块之后新增注入组配置**
```c
#ifdef USE_ADC_ZCD
    LL_ADC_INJ_SetTriggerSource(ADC1, LL_ADC_INJ_TRIG_EXT_TIM1_CH4);
    LL_ADC_INJ_SetTriggerEdge(ADC1, LL_ADC_INJ_TRIG_EXT_RISING);
    LL_ADC_INJ_SetSequencerLength(ADC1, LL_ADC_INJ_SEQ_SCAN_ENABLE_3RANKS);
    LL_ADC_INJ_SetSequencerRanks(ADC1, LL_ADC_INJ_RANK_1, BEMF_A_ADC_CHANNEL);
    /* rank2=BEMF_B, rank3=BEMF_C, 采样时间 6.5 cycles, 单端 */
#endif
```
**ADC.c — activateADC() ADC 使能后新增**
```c
#ifdef USE_ADC_ZCD
    LL_ADC_INJ_StartConversion(ADC1);
#endif
```

**(d) comparator.c — getCompOutputLevel() 改为非阻塞注入读 + blanking**
```c
uint16_t va = LL_ADC_INJ_ReadConversionData12(ADC1, LL_ADC_INJ_RANK_1);
uint16_t vb = LL_ADC_INJ_ReadConversionData12(ADC1, LL_ADC_INJ_RANK_2);
uint16_t vc = LL_ADC_INJ_ReadConversionData12(ADC1, LL_ADC_INJ_RANK_3);
uint16_t neutral = (va + vb + vc) / 3;
/* 依 adc_zcd_phase 选悬空相 bemf；blanking 窗口内返回上次极性不更新 */
```
`readADC_ZCD()` 阻塞函数保留（不再被调用），关闭 `USE_ADC_ZCD` 即整体回退。

#### 12.12.4 编译与验证状态

- **编译状态**：本次会话在当前 SDE/WSL 环境中**未能定位到 arm-none-eabi 工具链**（不在 PATH、`/usr/bin`、`/opt`、`/usr/local`、项目 `tools/`，且本地 ext4 根卷 `find -xdev` 全盘未命中）。经与用户确认，**暂缓编译**，改由用户在自己可正常编译的终端里构建单个目标：
  ```
  make CUSTOM_FD6288_G431
  ```
  （注意：只用目标名，不要用 `make all` 或 `make TARGET=...`，否则会编译全部目标。）
- **待验证项（拿到工具链后）**：
  1. 编译单目标 `CUSTOM_FD6288_G431` 通过，无 `-Werror` 告警（工程开启 `-Wall -Wextra -Werror`）。
  2. `arm-none-eabi-size` 检查 flash/RAM 占用在预算内。
  3. 上电观察：`zero_crosses` 应能连续增长、`commutation_interval` 收敛，电机脱离 ~50rpm 开环拖拽、可闭环加速；遥测 RPM 应平稳。
  4. 若启动阶段仍抖动，微调 `ADC_ZCD_BLANK_CALLS`（默认 3）与注入采样时间。

#### 12.12.5 回退方式

- 关闭 `USE_ADC_ZCD` 宏 → 回到原版硬件比较器 ZCD 路径（但本板引脚布局下硬件比较器不可用，仅作占位）。
- 或直接从备份恢复：源码改动集中在上述 4 个文件；文档备份见 `AM32-FD6288-G431-移植说明.md.bak-*`。

---

### 12.13 路线 C 首测复盘：注入触发边沿用反（RISING→FALLING）

#### 12.13.1 现象（烧入 Route C 固件后）

- 仍需关闭堵转保护才能起转；
- 低油门：电机低速抖动、慢转（与改前类似）；
- 高油门：电机变成**高频抖动**（改前没有这个表现）；
- 遥测/闭环仍未建立。

#### 12.13.2 日志证据（`AM32-FD6288-G431-routeC-1min-capture.log`）

日志每行字段：`idx va vb vc neutral bemf zero_crosses ci step running out phase`。

关键观察：**三相 BEMF 采样值 `va/vb/vc` 几乎恒为 0~3 counts**（个别行出现 60/108/119），而 12V 母线经 5.1k/1.0k 分压、在 ON 窗口内被驱动相的 ADC 读数应在 ~2400 counts 量级。

同时 `zero_crosses` 全程在个位~40 之间乱跳、`commutation_interval(ci)` 在 14000~28000 之间大幅波动不收敛——**说明注入组根本没有在 ON 脉冲中点采到有效电压，过零判定基本是噪声，闭环从未建立**，电机始终靠开环强拖（因此仍要关堵转、低速抖、高油门高频抖）。

#### 12.13.3 根因：注入组外部触发边沿选反了

TIM1 为**边沿对齐上计数**（`COUNTERMODE_UP`），CH4 为 `PWM1` 模式，故其输出参考信号 **OC4REF 在 `CNT < CCR4` 期间为高、在 `CNT == CCR4` 处跳低**。ADC 注入组以 `JEXTSEL = TIM1_CH4` 的 OC4REF 作为外部触发，按所选边沿采样：

| 触发边沿 | 触发时刻 | 采到的内容 |
|---|---|---|
| **RISING（原实现，错误）** | `CNT ≈ 0`（PWM 周期起点，高侧刚开通） | 死区 + 开关振铃瞬间 → BEMF ≈ 0（正是日志所见） |
| **FALLING（修正后，正确）** | `CNT == CCR4 == duty/2` | ON 脉冲**正中点**，最干净的采样点 |

上一版 `ADC.c` 里写的是 `LL_ADC_INJ_TRIG_EXT_RISING`，与"CCR4=duty/2 命中 ON 中点"的设计意图**自相矛盾**：CCR4 只决定 OC4REF 何时跳变，真正决定采样落点的是**边沿方向**。选 RISING 等于把采样钉在了周期起点（开关噪声区），所以读数恒 ~0。

#### 12.13.4 修正（单行改动 + 注释订正）

`Mcu/g431/Src/ADC.c` `ADC_Init()` 注入组配置：
```c
-  LL_ADC_INJ_SetTriggerEdge(ADC1, LL_ADC_INJ_TRIG_EXT_RISING);
+  LL_ADC_INJ_SetTriggerEdge(ADC1, LL_ADC_INJ_TRIG_EXT_FALLING);
```
同时把 `ADC_Init()` 与 `activateADC()` 里"rising-edge"的注释订正为 FALLING，并说明边沿选择依据（见文件内注释）。功能改动仅此一行。

改动前已备份：`Mcu/g431/Src/ADC.c.bak-20260725-233312`。

#### 12.13.5 预期效果与验证

- 修正后注入采样落在 ON 中点，`va/vb/vc` 应变为 ~1000~2500 counts 的真实分压值，悬空相 BEMF 过零可被正确检测。
- 预期 `zero_crosses` 能连续增长、`ci` 收敛，电机可脱离开环强拖进入 BEMF 闭环，堵转保护也应可重新打开。
- **需用户重新编译烧录验证**（本环境无 arm-none-eabi 工具链）：
  ```
  make CUSTOM_FD6288_G431
  ```
- 若修正后仍有残留抖动，再按顺序排查：
  1. 换相后消隐 `ADC_ZCD_BLANK_CALLS`（默认 3）是否够；
  2. 注入采样时间 6.5 cycles 是否偏短（分压源阻抗 ~0.83k，一般够，可试 12.5/24.5 cycles）；
  3. 低占空比启动段 CCR4=duty/2 过小导致采样落在死区内——必要时给 CCR4 设下限钳位。

---

### 12.14 边沿修复后复盘：BEMF 已正常，但反复失步 + 转速封顶 + 电流偏大

#### 12.14.1 现象（烧入 FALLING 修复固件后）

- 边沿修复生效：电机在 20% 以上油门能连续起转（此前完全转不动）。
- 残留问题：
  - 20% 以下油门仍低速抖动、无法稳定运转；
  - 20% 以上能转，但**转速无法随油门继续提高**（被钉住）；
  - 运行时**电流偏大**。

#### 12.14.2 日志证据（AM32-FD6288-G431-routeC-1min-capture.log，FALLING 版）

字段：`idx va vb vc neutral bemf zero_crosses ci step running out phase`。

- 采样已恢复正常：稳态行里驱动高相 va/vb/vc 之一 ~1550、下管相 ~15、悬空相 = 扫过中性点的 BEMF（约 400~1100），软件中性点 ~780~890。
  - 自洽校验：过零发生在 `bemf = (1550 + 15 + bemf)/3` -> `bemf ~ 782`，与日志中性点数值吻合。虚拟中性点 `(va+vb+vc)/3` 算法数学正确，采样点正确。RISING->FALLING 修复确认成功。
- **反复失步**：`zero_crosses` 在稳态连续累加（每条存样 +~180），但在第 17/19-22/31-32/35/63-67/97 行**被清回 1**，同时 `ci` 从稳态 ~10800 暴涨到 20000~34000 -> 典型的丢同步 / 重新捕捉循环。
- **转速封顶**：稳态 `ci` 几乎恒为 10600~11100 -> 电气换相周期被锁死 -> `e_rpm ∝ 1/e_com_time` 固定 -> 加油门转速不涨。

#### 12.14.3 根因分析（按可信度排序）

1. **主因（结构性）：注入组每个 PWM 周期只触发一次**（24kHz，约每 41.6us 一个 BEMF 点）。过零时刻被量化到 PWM 周期粒度：转速越高、每个换相步内可用采样点越少，过零时间抖动占比越大 -> 换相时序抖动 -> 到某转速守不住同步 -> 反复失步、转速封顶。时序偏差使换相不在最优点 -> 转矩角不对 -> 电流偏大。
2. **低油门抖动**：ON 窗口窄、`CCR4 = duty/2` 过小，采样点可能落进死区 / 开关振铃区，BEMF 采不干净 -> 抓不住同步。
3. 换相后消隐 `ADC_ZCD_BLANK_CALLS`（默认 3）与最小 BEMF 计数过滤，在采样点本就稀疏时会进一步吃掉观察窗，加剧漏检 / 迟检。

#### 12.14.4 下一步实验（按优先级，均为移植文件内的最小改动，不动 main.c）

- 实验 A（针对低油门抖动，最安全）：在 `SET_DUTY_CYCLE_ALL` 里给 `CCR4` 设一个下限钳位，保证采样点始终落在死区之后的 ON 稳定区（例如 `CCR4 = max(duty/2, DEADTIME_TICKS + margin)`）。
- 实验 B（针对反复失步）：把 `ADC_ZCD_BLANK_CALLS` 从 3 减到 1~2，扩大有效观察窗，减少漏检。
- 实验 C（提高时间分辨率，改动较大）：让注入组在 PWM 的两个边沿都触发（例如再用 TIM1 UPDATE 事件或 CH4 双边沿），把 BEMF 采样率翻倍，缓解过零时刻量化 -> 直接抬高失步转速上限。
- 实验 D（旁证）：确认 `TARGET_MIN_BEMF_COUNTS` 未设过大导致过零确认迟滞。

建议先做 A + B（单点、可回退），复测日志确认 `ci` 是否开始随油门下降、失步事件是否减少；若转速上限仍受限，再评估 C。

> 本环境无 arm-none-eabi 工具链，所有改动均需用户侧 `make CUSTOM_FD6288_G431` 编译烧录后复测。

---

### 12.15 实验 A+B：低油门采样点钳位 + 缩短换相消隐

针对 12.14 复盘中的"20% 以下抖动"与"反复失步"，本次实施 A+B 两处最小改动（均在移植文件内，未动 main.c 与闭环算法）。

#### 12.15.1 关键参数确认

- 目标板 `Inc/targets/CUSTOM-FD6288-G431.h`：`DEAD_TIME = 70`（TIM1 时钟节拍，PSC=0）。
- `TIM1_AUTORELOAD = 1999`，即 PWM 周期约 2000 ticks，占空区间约 0~2000。
- `SET_DUTY_CYCLE_ALL` 全部调用点均为独立语句形式（main.c / sounds.c 共 15 处），可安全改为带钳位的表达式。

#### 12.15.2 实验 A：CCR4 采样点下限钳位（Mcu/g431/Inc/peripherals.h）

问题：原 `CCR4 = duty/2`。低占空比时 ON 窗口窄，duty/2 落在高侧刚开通的死区+开关振铃区，注入采到的 BEMF 是噪声 -> 闭环锁不住（表现为 <20% 油门抖动）。

改动：新增 `static inline uint16_t zcd_ccr4_from_duty(uint16_t duty)`，`SET_DUTY_CYCLE_ALL` 改为调用它计算 CCR4。逻辑：

- `duty == 0` -> CCR4 = 0（停机不采样）；
- `duty <= ZCD_SAMPLE_FLOOR` -> CCR4 = duty/2（ON 窗口太窄无法钳位，退化为原行为，该区间本就由开环强拖负责）；
- 否则 CCR4 = `max(duty/2, ZCD_SAMPLE_FLOOR)`，并夹到 `<= duty-1` 以确保仍落在 ON 窗口内。

采样下限 `ZCD_SAMPLE_FLOOR = DEAD_TIME + 20 = 90` ticks（约 4.5% 占空），越过死区+振铃再采样。

> 说明：peripherals.h 新增了 `#include "targets.h"` 以取得 `DEAD_TIME`；targets.h 内的宏均由 `#ifndef/#define/#endif` 保护，重复包含幂等安全；`static inline` 每编译单元独立，无重复符号问题。

#### 12.15.3 实验 B：换相消隐从 3 降到 2（Mcu/g431/Src/comparator.c）

`#define ADC_ZCD_BLANK_CALLS 3u` -> `2u`。注入采样点本就稀疏（每 PWM 周期一次），过大的换相后消隐会吃掉有效观察窗、加剧过零漏检/迟检。降到 2 扩大观察窗，同时仍能挡掉换相瞬间的续流回扫尖峰。

#### 12.15.4 备份

- `Mcu/g431/Inc/peripherals.h.bak-20260726-000313`
- `Mcu/g431/Src/comparator.c.bak-20260726-000313`

#### 12.15.5 验证方法

本环境无 arm-none-eabi 工具链，请用户侧编译烧录：
```
make CUSTOM_FD6288_G431
```
复测重点（对比 12.14 日志）：
1. <20% 低油门是否能稳定运转、抖动是否减轻（A 的直接目标）；
2. 日志中 `zero_crosses` 被清回 1 的失步事件是否减少（B 的直接目标）；
3. `ci` 是否开始随油门增大而下降（转速能否随油门提高）。

若转速上限仍被钉住、加油门不涨，则确认为 12.14.3 主因（注入采样率量化），需进入实验 C（PWM 双边沿触发，采样率翻倍）。

---

### 12.16 A+B 复测：闭环已锁定！转速封顶是 AM32 低速限油门保护(low_rpm_throttle_limit)所致

#### 12.16.1 A+B 生效判定（AM32-FD6288-G431-routeC-3rd-capture.log，00:15 抓取）

对比 12.14 旧日志，A+B 后有决定性好转：

- **失步基本消除**：旧日志 `zero_crosses` 频繁被清回 1（每隔十几行一次失步）；新日志 `zero_crosses` 从 1 一路**单调累加到 14010**（104 条存样几乎不间断），仅 idx 9 / 25 出现两次极短的重同步（ci 一次性跳到 14746 / 21711 后立刻恢复 ~10800），失步率大幅下降。
- **换相序列健康**：`step`/`phase` 规整地 1->6->5->4->3->2 循环，`out` 极性随相位交替，`neutral` 稳定 ~700~900，三相分压值（~1550 / ~15 / 悬空 BEMF）干净。
- 结论：实验 A（采样点钳位）+ B（消隐 3->2）成功让 ADC 虚拟中性点 ZCD 闭环稳定锁定。这是移植以来第一次拿到连续不失步的 BEMF 闭环。

#### 12.16.2 但转速仍封顶：`ci` 恒定 ~10600~11200

新日志稳态 `commutation_interval` 仍恒在 10600~11200，不随时间下降 -> 转速被钉住。这不再是 ZCD 的问题（闭环已锁），而是 **AM32 主控逻辑主动把占空比压住了**。

#### 12.16.3 根因（代码坐实）：low_rpm_throttle_limit 低速限油门保护自锁

main.c 关键逻辑：

- line 376：`char low_rpm_throttle_limit = 1;`（默认开）。
- line 771-773：仅当 `motor_kv < 300` 才 `low_rpm_throttle_limit = 0`；本移植 KV 未特意设低，故保持为 1。
- line 2112-2119（每个换相周期执行）：
  ```
  duty_cycle_maximum = map(k_erpm, low_rpm_level, high_rpm_level,
                           throttle_max_at_low_rpm, throttle_max_at_high_rpm);
  ```
  其中 `low_rpm_level=20`、`high_rpm_level=70`（千电转/分）、`throttle_max_at_low_rpm=400`、`throttle_max_at_high_rpm=2000`。
- line 1309-1310：`if (duty_cycle_setpoint > duty_cycle_maximum) duty_cycle_setpoint = duty_cycle_maximum;` -> 无论油门给多大，占空比被 `duty_cycle_maximum` 钳死。

**自锁链**：转速未达 high_rpm_level(70k erpm) 前，duty_cycle_maximum 被 map 压低 -> 占空比上不去 -> 转速上不去 -> duty_cycle_maximum 继续被压低。系统卡在一个平衡点（日志 ci~10800），加油门转速不涨；同时低速下要维持该占空比，相电流偏大。这正好解释"20% 以上能转、转速不随油门涨、电流偏大"。

> 注意：这是 AM32 原生保护特性，不是移植 bug。它是为保护硬件/电机、限制低速大电流而默认开启的。

#### 12.16.4 下一步：配置层验证（优先，无需改代码）

low_rpm_throttle_limit 由电机 KV 与配置驱动。建议先用 **AM32 配置器 / DShot 参数**（或 eeprom 配置）调整，不动源码：

1. 正确设置 `motor_kv` 与 `motor_poles`：
   - 若实际电机 KV < 300，程序会自动关闭该保护（line 771-773）；
   - 否则 `low_rpm_level`/`high_rpm_level` 会按 KV 重新计算（line 774-775），把 high_rpm_level 设得更贴近电机实际最高电转，可让 duty 更早放开。
2. 确认 `throttle_max_at_low_rpm`（默认 400）是否过低——它决定低速段允许的最大占空比。
3. 复测：低油门给定后观察 `ci` 是否能随油门增大而下降、转速是否跟随。

若确认就是该保护限制、且希望源码级永久放开（需你同意后再改，仍不动闭环算法）：可在目标头文件 `Inc/targets/CUSTOM-FD6288-G431.h` 增加编译期覆盖（例如提高 high_rpm_level 或直接令 low_rpm_throttle_limit 默认关闭），属于配置项而非算法改动。

#### 12.16.5 与实验 C 的关系

转速封顶已确认由 low_rpm_throttle_limit 造成，**不是**注入采样率量化（12.14.3 主因推断在此被日志修正）。因此**暂不需要实验 C**（PWM 双边沿）。先做 12.16.4 的配置层验证。

> 本环境无 arm-none-eabi 工具链，源码级改动仍需用户侧 `make CUSTOM_FD6288_G431` 编译烧录。

## 12.17 电池电压是否触发保护 / 影响限速（问题排查）

### 问题
用户提问："是不是现在 AM32 不知道实际电池电压，所以会保护一些东西？"
即：当前的转速被限住、电流偏高，是否与电压检测不准导致的保护有关。

### 结论（先给答案）
- 当前的转速被限死（ci 稳定在 ~10800、油门加大转速不涨、电流偏高）与电池电压无关。
  真正的原因是 AM32 原生的 `low_rpm_throttle_limit` 转速限幅（见 12.16），它只用
  电转速 `k_erpm`，完全不读 `battery_voltage`。
- 低压保护 `low_voltage_cut_off` 默认关闭，且受 EEPROM 配置项 gate，当前没有触发。
- 但是电压确实"测不准"：本 target 没有定义 `TARGET_VOLTAGE_DIVIDER`，代码回退到默认 110，
  而本板 PA0 分压网络与之不一定匹配，导致 `battery_voltage` 标定偏差。这不会限速，
  但会影响自动电芯数判断、遥测显示等。

### 依据（代码定位）
1. 低压保护路径（Src/main.c）：
   - line 340: `char LOW_VOLTAGE_CUTOFF = 0;` 默认关闭。
   - line 2061: `if (eepromBuffer.low_voltage_cut_off == 1)`（每芯欠压）
   - line 2070: `if (eepromBuffer.low_voltage_cut_off == 2)`（绝对欠压）
   - 均受 EEPROM 配置项 gate，默认不生效 -> 不是当前限速/大电流的原因。
2. 转速限幅（12.16 已确认）：
   - line 376: `char low_rpm_throttle_limit = 1;`（默认开）
   - line 2112: `duty_cycle_maximum = map(k_erpm, low_rpm_level, high_rpm_level, throttle_max_at_low_rpm, throttle_max_at_high_rpm);`
   - 输入只有 `k_erpm`，不含 `battery_voltage` -> 与电压无关。
3. 电压相关的斜坡 `VOLTAGE_BASED_RAMP`：
   - Src/main.c line 1453: `#ifdef VOLTAGE_BASED_RAMP` -> `voltage_based_max_change = map(battery_voltage, 800, 2200, 10, 1);`
   - `VOLTAGE_BASED_RAMP` 仅在 targets.h line 849 (`RR_ROBOT_DUAL_ESC_F421`) 和
     line 2222 (`REPEAT_DRIVE_F051`) 两个别的 target 的 `#ifdef` 块内定义。
   - 本 target CUSTOM-FD6288-G431 未定义该宏 -> 这段电压相关的斜坡逻辑对本板不编译、不生效。
     即使电压测不准，也不会通过这条路径影响油门斜坡。
4. 分压系数：
   - Src/main.c line 381: `uint16_t VOLTAGE_DIVIDER = TARGET_VOLTAGE_DIVIDER;`
   - CUSTOM-FD6288-G431.h 未定义 `TARGET_VOLTAGE_DIVIDER`，回退 targets.h 默认 110。
   - line 2055: `battery_voltage = ((7*battery_voltage) + ((ADC_raw_volts*3300/4095*VOLTAGE_DIVIDER)/100)) >> 3;`
   - 分压系数错 -> battery_voltage 数值不准（但不限速、不触发已关闭的低压保护）。

### 电压不准的实际影响面（不影响限速）
- 自动电芯数判断（cell_count）：line 1350 附近，仅在 low_voltage_cut_off==1 时用，默认关闭。
- 遥测/显示电压：DShot telemetry 上报的电压会偏。
- 若将来手动打开 low_voltage_cut_off，会因标定错误而误触发或不触发。

### 建议（可选，属配置层，不改算法）
1. 在 CUSTOM-FD6288-G431.h 中按实际分压网络定义 `TARGET_VOLTAGE_DIVIDER`，
   使 battery_voltage 与真实电压对齐（便于遥测和后续启用低压保护）。
   本板电压检测在 PA0，分压 5.1k(上) / 1.0k(下)，比值 (5.1+1.0)/1.0 = 6.1。
   VOLTAGE_DIVIDER 的语义为每 100 计数对应的倍率，参考值可按 6.1 -> 约 610 试算，
   实测后微调（用已知电池电压反推）。
2. 解决"转速被限死"仍走 12.16 的路线：正确设置 motor_kv / motor_poles（KV<300 会
   自动关闭 low_rpm_throttle_limit），或按需在配置层放开限幅参数。这与电压无关。

## 12.18 方向性修正：转速上不去不是原生限速，而是软件 ADC-ZCD 的采样/相位问题

### 触发本节的关键反证（用户提供）
用户用同一台电机在**其他标准 AM32 电调**（硬件 COMP 方案）上驱动，除启动功率要稍大外，能正常跑到目标转速，且**没有遇到限速保护**。
标准 AM32 电调同样默认 `low_rpm_throttle_limit = 1`。既然标准电调带着同样的保护都能正常跑，
那么 12.16 把"转速封顶"归因于 `low_rpm_throttle_limit` 的结论**方向就错了**。

### 结论修正
- 撤销 12.16 的主结论。`low_rpm_throttle_limit` 是所有 AM32 通用逻辑，不是本板独有，
  不能解释"本板跑不动而标准电调能跑"的差异。
- 真正的差异只可能在**我们移植时改动的部分**：即把硬件 COMP + EXTI 过零，换成了
  ADC 注入组轮询 + 软件虚拟中性点的 ZCD（USE_ADC_ZCD 分支）。差异根因锁定在
  **采样时机 / 过零检测的相位误差**。

### 为什么软件 ADC-ZCD 的相位误差会表现为"转速上不去 + 电流偏大"
AM32 六步梯形控制的时序假设（Src/main.c）：
- 理想过零点位于两次换相的正中间，即换相后 30 电角度。
- 检测到过零后，再等**半个 commutation_interval**（约 30 度）才 commutate()。
- 换相时刻 = 过零时刻 + waitTime；commutation_interval 由相邻两次过零间隔一阶低通得到。

如果软件 ZCD 相对真实过零存在**系统性延迟或抖动**（ADC 注入 3 相串行转换 ~1.4us、
每个 PWM 周期只在 ON 中点采一次即时间量化 ~41.6us、虚拟中性点重构噪声、换相消隐窗口
遮蔽了真实过零附近的样本），那么：
- 检测到的过零时刻整体偏晚 -> 换相时刻整体滞后 -> 换相相位偏离最优点
- 相位不对 -> 产生的转矩下降、无功电流上升 -> 表现为"电流偏大、转速上不去"
- 越往高速，单位电角度对应的时间越短，固定的采样时间量化误差占的电角度越大 ->
  高速端相位误差被放大 -> 明显封顶

这与用户"某些采样时机不准确"的直觉一致，是当前最可疑的根因。

### 复盘中发现的一个数值前提错误（影响 Experiment A 的参数标定）
- 之前一直假设本 target 的 TIM1 ARR = 1999。**这是错的**。
  1999 是 `MCU_CH32V203` 块（targets.h:5374）里的值；G431 块（5216-5252）未定义 TIM1_AUTORELOAD，
  走 fallback（5411-5414）：`ARR = CPU_FREQUENCY_MHZ*1e6/NOMINAL_PWM - 1 = 160e6/24000 - 1 = 6665`。
- 且传给 SET_DUTY_CYCLE_ALL 的是 `adjusted_duty_cycle = duty_cycle * tim1_arr / 2000 + 1`
  （main.c:1489），范围是 0..tim1_arr(6665)，**不是** 0..2000。
- VARIABLE_PWM 开启时 tim1_arr 还会随转速在 [TIMER1_MAX_ARR/2, TIMER1_MAX_ARR] 间动态变化（main.c:1894）。
- 影响：Experiment A 里 `ZCD_SAMPLE_FLOOR = DEAD_TIME + 20 = 90` 是个**固定 tick 值**，
  它是在"ARR=1999"的错误认知下调出来的经验值。在真实 ARR=6665、且 ARR 会动态缩放的框架下，
  这个固定 floor 的实际含义（占 ON 窗口的比例、对采样点的偏移）与设计意图不符。
  也就是说 Experiment A/B 很可能是"在错误的坐标系里打的补丁"，即便当时 log 看起来改善，
  也不能保证在中/高速段采样点仍落在正确位置。

### 采样点应当遵循的正确原则（后续修复方向，先记录不改代码）
1. 采样必须在**高边 ON 且开关振铃已衰减**之后进行（躲过 DEAD_TIME + 建立时间）。
2. 采样点应表达为**占当前 ON 窗口的比例**（例如 ON 中点 = adjusted_duty/2），
   而不是固定 tick，才能在 ARR 动态缩放时始终落在 ON 中段。
3. 消隐（blanking）只用于遮蔽换相瞬间的续流尖峰，窗口要"够用即可"，
   过长会把真实过零附近的样本也遮掉，造成过零检测偏晚（相位滞后）。
   Experiment B 把 3->2 是往正确方向走，但应以"刚好覆盖续流尖峰"为准，靠实测定。
4. 若要根治相位误差，考虑对浮空相做**两点或多点采样 + 线性插值**求过零时刻，
   而不是只靠单点比较 + 计数阈值（见下节对过采样的评估）。

### 建议的验证顺序（不改算法，先量化）
1. 用 12.4 的 GDB dbg_buf 抓取，改为记录"检测到过零时的 tim1_arr / adjusted_duty / CCR4 实际值"，
   确认中/高速段采样点占 ON 窗口的比例是否仍然合理。
2. 示波器同时看某相 SW 节点电压与一个用于打标的 GPIO（在 commutate() 和 过零判定处翻转），
   直接量出"真实过零 -> 检测过零 -> 换相"的相位关系，看是否系统性滞后。
3. 先把采样点从固定 floor 改回"按 adjusted_duty 比例定位"，再逐步收窄 blanking，观察电流是否下降、转速是否能上去。

## 12.19 移植改动清单复核（找多余/方向错误的修改）+ 过采样想法评估

### A. 改动清单复核（相对原始 AM32）
按"移植必需 / 调试残留 / 可疑(可能有害或已过时)"三类标注。

必需（硬件相关，保留）：
- comparator.c 的 USE_ADC_ZCD 分支：软件虚拟中性点 ZCD，替代本板不可用的硬件 COMP2
  （PA2 与 PA5 都接在 COMP2 负输入，硬件方案无法工作）。属结构性必需。
- ADC.c 注入组配置：TIM1_CH4 触发、3 相 rank A/B/C、6.5 周期采样。必需。
- ADC.c 注入触发边沿 RISING->FALLING（12.13）：必需且正确（让采样落在 ON 中点而非 CNT=0 噪声区）。
- CUSTOM-FD6288-G431.h 的引脚/外设映射：必需。

调试残留（不影响功能，但建议最终清理）：
- comparator.c 的 zcd_dbg_* 全局、dbg_buf 每秒采集、zcd_dbg_calls/flips 计数：纯诊断，
  验证完成后应移除或用编译开关包起来，避免长期占用 RAM 和 CPU。

可疑 / 方向可能错误（本次复核重点）：
1. peripherals.h 的 zcd_ccr4_from_duty() + ZCD_SAMPLE_FLOOR（Experiment A）：
   在错误的 ARR 前提（以为 1999，实为 6665）下标定的固定 tick floor=90。
   在真实框架 + VARIABLE_PWM 动态 ARR 下，采样点比例不受控，属于"可能有害/已过时"，
   应改为按 adjusted_duty 比例定位（见 12.18）。这是当前最需要重做的一处。
2. comparator.c 的 ADC_ZCD_BLANK_CALLS 3->2（Experiment B）：方向对（缩短消隐），
   但数值是拍脑袋定的，应以示波器实测续流尖峰宽度为准，不能凭 log 观感定。
3. changeCompInput() 里重新武装 blanking 的时机：需确认它是在 commutate() 之后、
   且与过零计数阈值配合得当，否则也会引入相位偏差（本次未逐行验证，列为待查）。

结论：没有发现"会导致进不了主程序"的方向性错误残留（那类问题在第八/十章 bootloader/old_routine
已解决）。当前唯一明确"方向可能错"的活动改动，就是 Experiment A 的固定 tick 采样点钳位。

### B. 对"MCU 利用率低，玩命过采样/超采样获取完整 BEMF 曲线"的评估
你的直觉方向是对的（采样时机是关键），但要区分两种目标：

1. 在 AM32 现有的六步梯形框架内：
   - 六步换相在数学上**只需要一个信息点：过零时刻**。得到完整 BEMF 曲线，对"何时换相"
     这个决策的边际收益有限——真正有用的是把过零"时刻"测得更准、更少延迟。
   - 因此现实可行且高性价比的做法不是"存整条曲线"，而是：
     (a) 在浮空相过零附近做**两点采样 + 线性插值**，把过零时刻精确到 PWM 周期以内，
         消除"每周期只采一次"带来的时间量化误差（当前 ~41.6us，高速时相位误差显著）；
     (b) 用插值得到的亚周期过零时刻去喂 commutation_interval，相位更准 -> 电流下降、转速能上去；
     (c) 适度提高每个 ON 窗口内的采样次数用于抗噪（中值/多数表决），但没必要"玩命"。
   - 代价：改 comparator.c 的过零判定逻辑（从单点比较+计数，改成插值求时刻），
     属于对 ZCD 层的重写，但**不动 main.c 的换相时序主框架**，风险可控。

2. 若真想"利用完整 BEMF 曲线让电调更好控制电机"：
   - 那本质上是走向 **FOC / 正弦驱动**（连续估计转子角度，而非只找 6 个过零点）。
   - 这已超出 AM32 六步架构，等于换控制算法，工作量和风险都大，不建议在当前排障阶段做。
   - AM32 自带的 sine/FOC-ish startup（stepper_sine）只是启动辅助，不是稳态 FOC，不能等同。

### C. 务实建议（下一步，先量化再改）
1. 先按 12.18 的验证顺序，用示波器/GDB 量出"真实过零 -> 检测过零 -> 换相"的相位关系，
   确认是否系统性滞后。这是判断根因、也是验证后续修改是否有效的基准。
2. 第一刀改动（最小、方向明确）：把 zcd_ccr4_from_duty 从固定 tick floor 改为
   "按 adjusted_duty 比例定位 ON 中点 + 一个以比例表达的最小躲避 DEAD_TIME 余量"，
   让采样点在 ARR 动态缩放时始终落在 ON 中段。改完复测电流与转速。
3. 若相位滞后仍在，再上"过零两点插值"（B.1.a），这是收益最大的一步。
4. 过采样存整条曲线/转 FOC：当前阶段不做，记录为远期可选项。

## 12.20 Experiment D：把消隐/过零判定绑定到"真实新采样"（JEOS 门控）

### 为什么改这里（不是改 Experiment A 的采样点）
复核 comparator.c + main.c 后，定位到一个比"采样点位置"更根本的架构问题：
- 主循环 getBemfState()（main.c:817）以远高于 PWM 频率的速度反复调用 getCompOutputLevel()。
- 但注入组 ADC 只由 TIM1_CH4 在每个 PWM 周期触发一次（24kHz -> 约 41.6us 才更新一次 BEMF）。
- 原实现的消隐 adc_zcd_blank 是按"函数调用次数"计的（Experiment B: 2 次）。主循环里 2 次调用
  往往只隔几微秒，**远不足以覆盖换相后的续流(flyback)暂态**（该暂态约一个 PWM 周期量级）。
- 后果：续流尖峰被当成过零 -> 提前换相 -> 换相相位偏早 -> 转矩下降、电流上升、转速封顶。
  这正好解释"闭环能锁但转速上不去、电流偏大"，也与用户"采样时机不准"的直觉吻合。

顺带修正认识：Experiment A 的固定 tick floor(=90) 只在极低 duty(<约3%)才介入，正常油门下
zcd_ccr4_from_duty 取的就是 duty/2(ON 中点)，所以它在正常运行时基本惰性，**不是**高速跑不动的
主因，本次不动它，避免"为改而改"。

### 改动内容（仅 comparator.c，已备份，可回退）
1. 新增 ADC_ZCD_BLANK_SAMPLES(=2)：消隐单位从"调用次数"改为"真实新采样个数"(= 真实 PWM 周期)。
2. getCompOutputLevel() 顶部加 **fresh-sample gate**：
   - 若 `LL_ADC_IsActiveFlag_JEOS(ADC1)` 未置位 -> 说明自上次以来没有新的注入序列完成 ->
     直接 `return adc_zcd_last_out`，不重读、不动消隐计数、不重复计数同一采样。
   - 若已置位 -> `LL_ADC_ClearFlag_JEOS(ADC1)` 后再读 JDR1/2/3，正常处理。
   这样消隐计数只在"真的来了新采样"时递减，2 个样本 = 2 个 PWM 周期 ≈ 83us 的确定物理时间窗，
   与主循环轮询速度解耦。
3. changeCompInput() 里换相后 `adc_zcd_blank = ADC_ZCD_BLANK_SAMPLES` 并 `LL_ADC_ClearFlag_JEOS(ADC1)`，
   丢弃可能跨在换相沿上的半旧样本，让消隐窗从干净的下一个采样开始。

安全性：JEOS 属 ADC 注入组，DMA 常规组(温度/电压遥测)用的是常规组 EOC/EOS，两者独立；
grep 确认应用层无任何其它代码引用 JEOS。因此本改动不影响遥测，也不新增中断。

### 无示波器时的验证方法（用现成的 dbg_buf + 手感 + 遥测）
1. 编译烧录（只编 `make CUSTOM_FD6288_G431`）。
2. 用第 12.4 节的 GDB 读 dbg_buf：对比改动前后同一油门下——
   - zero_crosses 是否更平滑单调增长（相位更稳）；
   - 若加载了电流表/电源限流显示：**同一油门下母线电流应下降**；
   - 达到的 ci(commutation_interval) 应能比之前更小（= 转速更高）。
3. 手感/听感：给同一中等油门，电机应能明显比之前转得更快、更顺，不再"闷、烫、上不去"。
4. 若仍偏早/偏晚，只需调 ADC_ZCD_BLANK_SAMPLES：
   - 电流仍大、疑似仍采到续流尖峰(提前换相) -> 试 3；
   - 高速端丢步/失锁(消隐太长吃掉真过零) -> 试 1。
   一次只改这一个数，便于二分定位。
5. 回退：如无改善或变差，用本节备份 `Mcu/g431/Src/comparator.c.bak-*` 覆盖即可。

### 若这步仍不够（下一步预案，暂不做）
按 12.19-B 的建议，在过零点附近做两点采样 + 线性插值求过零"时刻"，消除"每周期一次"的时间量化误差。
这是收益最大的下一刀，但改动更大，等本次 JEOS 门控实测结果后再决定是否推进。


---

## 12.21 关于"采样时机是否随转速变化"与"仍需开环强拉、20% 以下震荡"的分析

本节回答两个问题：
(1) Experiment D 改后的采样时机能否跟随转速上升而变化？
(2) 为什么现在仍然只能"关闭堵转保护 + 开环强拉"才能转起来，且 20% 以下油门震荡严重？并给出下一步建议。

### 12.21.1 Q1：采样时机能否随转速变化

要把"采样时机"拆成两个独立部分来看：

**A. 采样点在 PWM 周期内的位置（CCR4）——会随占空比自动变化，正确。**
- CCR4 由 `zcd_ccr4_from_duty()` 计算，正常工作区约等于 `duty/2`，即始终落在 PWM 高电平（ON 脉冲）的中点。
- 占空比越大，ON 脉冲越宽，CCR4 也随之后移，采样点始终锁在 ON 脉冲中点。这是我们想要的：在上桥导通、下桥续流稳定的时刻采 BEMF。
- 所以"采样点相对 PWM 波形的位置"是随占空比自适应的，这一部分没问题。

**B. 消隐窗口（blanking）——现在按"新采样个数"计，会随 PWM 周期缩短而缩短，但不是严格按换相周期比例缩放。**
- Experiment D 把消隐从"getCompOutputLevel() 调用次数"改成了"新到的注入采样个数"（`ADC_ZCD_BLANK_SAMPLES = 2`），即换相后跳过 2 个真实 PWM 周期的采样。
- 本目标开启了 VARIABLE_PWM：`tim1_arr = map(commutation_interval, 96, 200, TIMER1_MAX_ARR/2, TIMER1_MAX_ARR)`。转速升高 → commutation_interval 变小 → tim1_arr 变小 → PWM 频率升高 → PWM 周期变短。
- 因此"2 个 PWM 周期"对应的绝对时间会随转速升高自动缩短——从这个意义上说，消隐时间确实会跟随转速变化，方向也是对的（高速时续流暂态本身也更短）。

**但要注意一个 caveat（这也是潜在的隐患）：**
- 消隐是按"固定 PWM 周期个数"缩放的，而不是按"换相周期的固定比例"缩放的。
- VARIABLE_PWM 只在 [TIMER1_MAX_ARR/2, TIMER1_MAX_ARR] 之间变化，PWM 周期最多缩短一半（频率最多翻倍）；而换相周期（电周期）从启动到高速可以变化几十倍。
- 结果：低速时 2 个 PWM 周期占换相周期的比例很小（消隐几乎可忽略）；高速时换相周期变得很短，2 个 PWM 周期占换相周期的比例反而变大，可能吃掉一部分真正的 BEMF 观察窗。
- 目前 `ADC_ZCD_BLANK_SAMPLES = 2` 是个折中值。如果后续高速端出现观察窗不足，可以考虑把消隐改成"按 commutation_interval 的固定比例"动态计算（例如换相周期的 1/8），而不是固定 2 个周期——但这属于进一步优化，先不动，待实测数据支撑。

**Q1 小结：** 采样点位置随占空比自适应（正确）；消隐时间随 PWM 周期（因而随转速）自动缩短（方向正确），但它是按固定周期数而非换相周期比例缩放，两端极限工况下比例会漂移。当前实现对中间转速区是合理的。

### 12.21.2 Q2：为什么仍需开环强拉、20% 以下震荡严重

"必须关堵转保护 + 开环强拉才能起转，且低油门震荡"这个症状，指向的核心是：**低速闭环零点检测（ZCD）仍然不可靠**。既然同一个电机在标准 AM32 电调上能正常闭环起转和上速，说明电机、AM32 算法本身没问题，问题在我们这块板子的 BEMF 采样链路。逐条排查：

**(1) 首要新怀疑点：分压网络是"交叉耦合"型，而软件中性点重构假设的是"独立简单分压"——数学模型不匹配。**
- 看 `CUSTOM-FD6288-G431.h` 的注释所描述的实际硬件：
  - PA0 = 5.1K 接 A 相 + 7.1K 接 B/C 相 + 1K 下拉到 GND
  - PA1 = 5.1K 接 B 相 + 7.1K 接 A/C 相 + 1K 下拉到 GND
  - PA2 = 5.1K 接 C 相 + 7.1K 接 A/B 相 + 1K 下拉到 GND
  - PA5 = 7.1K 接 A/B/C + 1K 下拉（板载真实中性点候选，但当前软件未使用它）
- 而 `comparator.c` 里的注释与数学假设的是"三路完全相同的简单 5.1k/1.0k 独立分压"，中性点用 `(Va+Vb+Vc)/3` 重构。
- **问题**：交叉耦合网络下，每个 ADC 引脚读到的不是"单一相经简单分压"，而是"本相 + 另外两相"的加权混合。此时 `(Va+Vb+Vc)/3` 得到的并不是真正的电机中性点电位，浮空相读数与这个"伪中性点"的比较关系会被扭曲。
- 这种扭曲在**高速时**因 BEMF 幅值大、信噪比高，可能还能勉强过关（这也解释了为什么开环拉起来、速度上去后能维持）；但在**低速时** BEMF 幅值很小，中性点重构的系统性误差就足以淹没真正的过零信号，导致 ZCD 抖动/误判 → 无法自主起转、低油门震荡。
- **这是当前最值得先验证/修正的疑点。** 需要用实际电阻网络推导正确的中性点/过零判据，或者干脆改用板载 PA5（真实中性点候选）作为参考再比较，而不是软件 (Va+Vb+Vc)/3。

**(2) 低速本身信噪比差是普遍难点，AM32 原生有对应机制，需确认没被我们改动削弱。**
- AM32 原生低速措施：`min_bemf_counts` 在 `zero_crosses < 5` 时翻倍（×2=6），要求连续更多次一致才认过零；`min_startup_duty = 120` 保证启动有足够 BEMF；line 1300 起转时的功率提升；`filter_level = 5` 的换相周期低通滤波。
- 这些原生机制是好的、应保留。我们的 Experiment D 消隐若过长，会与"连续计数"叠加吃掉观察窗，反而更难在低速凑够计数——但目前 blank=2 应该不至于。这一点需要靠 dbg_buf 抓的低速数据确认。

**(3) 现象与"中性点误差"高度吻合的判据。**
- 如果是中性点重构错误，dbg_buf 里在低速稳态下应看到：`bemf` 与 `neutral` 的交叉点相对换相边沿有明显系统性偏移（不是对称居中），或者 `out` 翻转位置随占空比漂移。
- 反之如果只是纯噪声/幅值不足，交叉点会是随机抖动而非系统性偏移。
- 建议下次实测把 dbg_buf 在"刚好能开环维持的最低转速"下抓一段回读，看交叉点是系统性偏移还是随机抖动，即可区分 (1) 与 (2)。

### 12.21.3 Comment 与下一步建议（待你确认方向后再动代码）

**我的总体判断：** 当前"开环能拉起、闭环低速失败"的最可能根因是 **12.21.2(1) 的交叉耦合分压网络与软件中性点数学不匹配**，而不是采样时机（Experiment D 的时机改动方向是对的）。采样时机只是"何时采"，中性点数学是"采到后怎么判过零"——后者错了，时机再准也没用。

**下一步可选方案（按优先级）：**
1. **【首选，最小改动】** 依据 `CUSTOM-FD6288-G431.h` 注释的真实电阻网络，重新推导正确的过零判据。两条子路线：
   - (a) 用板载 PA5（7.1K×3 + 1K，本就是三相对称求和，最接近真实中性点）作为中性点参考，把注入组第 4 rank 采 PA5，比较 `bemf_floating vs V(PA5)`，替代软件 (Va+Vb+Vc)/3。这更贴近硬件本意。
   - (b) 若坚持软件重构，则按交叉耦合矩阵反解出等效相电压再取平均——数学更复杂、更易出错，不推荐。
2. **【验证手段，不改控制逻辑】** 先用现有 dbg_buf 在最低可维持转速抓数据，确认是"系统性偏移"（支持方案 1）还是"随机抖动/幅值不足"（则需重点调启动功率/滤波）。
3. **【时机侧的进一步优化，暂缓】** 若高速端观察窗不足，把消隐从固定 2 个 PWM 周期改为按 commutation_interval 固定比例动态计算。低速问题解决前不动这个。

请确认你希望走 **方案 1(a)（改用 PA5 真实中性点参考）** 还是先做 **方案 2（抓 dbg_buf 数据判定根因）**，我再据此改代码。在此之前不改动 comparator.c 的判据逻辑，避免"为改而改"。


### 12.21.4 更正：分压网络是独立简单分压，非交叉耦合（12.21.2(1) 作废）

用户指出并复核确认：`CUSTOM-FD6288-G431.h` 注释里的 "5.1K to A, 7.1K to B/C" 并不是"每个引脚有一条物理电阻耦合到他相"，而是：

- 每相各自一条独立链路：`相X --5.1K--> PAi --1.0K--> GND`，三条彼此独立。
- 注释里的 "7.1K to 他相" 是**经 GND 的间接等效路径**：从 PAi 出发 `1.0K(本相下拉) + 1.0K(他相下拉) + 5.1K(他相串阻) = 7.1K`，是寄生/等效描述，不是实际的相间耦合电阻。

**结论修正：**
- 三路 BEMF 分压是**完全相同的独立 5.1K/1.0K 简单分压**，增益均为 `1.0/(5.1+1.0) = 0.164`。
- 因此 `comparator.c` 用 `neutral = (Va+Vb+Vc)/3` 重构中性点在数学上是**成立且尺度一致的**——12.21.2(1) 提出的"交叉耦合导致中性点数学不匹配"判断**作废**。

**方案 1(a)（改用 PA5 作中性点）也一并撤回：**
- PA5 是 `7.1K x3 汇流 + 1.0K 下拉` 的求和节点。解节点方程：
  `Vn = (Va+Vb+Vc)/(7.1*(1+3/7.1)) = (Va+Vb+Vc)/10.1 ≈ 0.297 * avg(V_phase)`。
- 而相分压 ADC 读数 `V_adc = 0.164 * V_phase`，软件中性点 `(Va_adc+Vb_adc+Vc_adc)/3 = 0.164 * avg(V_phase)`。
- 两者增益不同（0.297 vs 0.164），直接比较 `bemf_adc vs V(PA5)` 会有**恒定尺度失配**（PA5 恒偏高约 1.8 倍），除非再乘系数校正。所以 PA5 并不比软件 (Va+Vb+Vc)/3 更好，方案 1(a) 撤回。软件重构保持不变。

**低速根因重新聚焦（既然中性点数学是对的）：**
1. **信噪比/幅值**：低速 BEMF 本就小，需确认 ADC 采样点、增益、消隐没有削弱有效信号。这是最普适的低速难点。
2. **采样时机与续流暂态**：Experiment D 的消隐（blank=2 PWM 周期）是否恰当，需 dbg_buf 实测在最低可维持转速下看 bemf/neutral 交叉是否干净。
3. **启动参数**：min_startup_duty=120、line 1300 起转功率提升、filter_level 等原生量是否适配本电机/本板（同电机在标准电调 OK，说明是参数或链路细节，不是算法）。
4. **共模/采样相位**：三相同时采样时刻是否都在同一 PWM ON 中点、是否受 PWM 相位/死区影响导致三路不等价。

**下一步建议（不变）：** 优先做 **方案 2**——用现有 dbg_buf 在"刚好能开环维持的最低转速"抓一段回读，看 bemf 与 neutral 的交叉是干净的系统性过零，还是随机抖动/幅值不足。据此再决定调时机、调启动功率、还是调滤波，避免盲改。


---

## 12.22 dbg_buf 数据抓取与解析工具（方案 2）

为判定低速根因（系统性偏移 vs 随机抖动/幅值不足），提供两个脚本，均放在 `tools/`：

- `tools/dump_dbg.py`：GDB 内嵌 Python 脚本，把 comparator.c 里的 `dbg_buf` 通过 SWD 导出成 CSV。
- `tools/plot_dbg.py`：离线解析 CSV，打印诊断指标并画三张图（原始 va/vb/vc + neutral；bemf vs neutral；margin=bemf-neutral 并标注 out 翻转）。

### 12.22.1 重要前提：默认抓取速率太慢，看不到过零细节

当前 comparator.c 的 `dbg_buf` 是 **每约 1 秒存 1 条**（`if (++dbg_skip >= 16800)`），512 条要 8.5 分钟，只能看长期趋势，**无法看到单个换相周期内 bemf/neutral 的过零形状**。要区分"系统性偏移 vs 随机抖动"，必须让采样密到每个换相周期有多点。

**可选的快速捕获改动（一行，实验性，抓完即回退）：**
把 comparator.c 里的
```c
if (++dbg_skip >= 16800) {
```
临时改成（例如每 4 次调用存 1 条，512 条约覆盖几十个换相周期，具体倍率按低速时 getCompOutputLevel 调用率与换相周期估）：
```c
if (++dbg_skip >= 4) {   /* TEMP fast capture for 12.22 low-speed ZCD diagnosis; revert to 16800 after */
```
注意：
- 这是**临时诊断改动**，抓完数据后改回 16800（或用宏包起来）。
- 快速存储会更频繁写 buffer，但 dbg_buf 只是内存写，不影响控制路径时序（除多几条内存写外）。
- 512 条填满即停（buffer 不回卷），所以要在目标转速稳定后再触发/复位捕获（可在 GDB 里手动 `set var dbg_idx=0` 重置后再让电机进入目标低速，然后停下 dump）。

### 12.22.2 抓取步骤（motor 侧）

1. 编译烧录（只构建本目标）：`make CUSTOM_FD6288_G431`。
2. 用 GDB 通过 SWD 连接目标（OpenOCD / ST-Link / pyOCD 均可），例如：
   ```
   arm-none-eabi-gdb build/CUSTOM-FD6288-G431.elf
   (gdb) target extended-remote localhost:3333
   ```
3. 让电机运行到"刚好能开环维持的最低转速"，稳定几秒。
4. （可选）重置 buffer 从干净起点抓：`(gdb) set var dbg_idx = 0`，再让它跑到填满（快速模式下很快）。
5. 暂停并导出：
   ```
   (gdb) source tools/dump_dbg.py
   (gdb) dumpdbg dbg_capture.csv
   ```
   脚本按字段名逐个读取 struct dbg_entry，无需关心对齐/字节序。motor 建议先停，避免读到正在写的 buffer。

### 12.22.3 解析步骤（PC 侧）

```
python3 tools/plot_dbg.py dbg_capture.csv
# 或无界面环境保存图片：
python3 tools/plot_dbg.py dbg_capture.csv --no-show --save fig.png
```
脚本会打印：
- `margin (bemf-neutral)` 的 mean / min / max / peak-peak；
- `out polarity flips` 与 `margin sign changes`；
- `mean/amp ratio`（DC 偏置占摆幅比例）。

### 12.22.4 判读准则

- `|mean|/amp` 小，且 `out flips` ≈ `margin sign changes`，margin 干净地穿越 0 → **ZCD 干净**，低速问题多半在启动功率/时序参数，而非采样链路。
- `|mean|/amp` 大（margin 有持续 DC 偏置，浮空相期间始终偏向一侧）→ **系统性偏移**，需查采样时刻共模、三相采样是否等价、消隐是否吃掉半边波形。
- margin peak-peak 很小且 flips 很多（抖动）→ **低信噪比/幅值不足**，需提高启动/低速功率或加滤波。

这一步只读数据、不改控制逻辑（除 12.22.1 的临时抓取速率外），据结果再决定下一处最小改动，避免盲改。



---

## 12.24 WSL 工具链安装与 SWD 调试环境搭建

目标：在这台无 root 权限的公司 WSL(Ubuntu 24.04) 上装好 AM32 固件编译器 + OpenOCD + GDB，并连接 Windows 上的 ST-Link 调试 STM32G431。

### 12.24.1 环境约束（实测）

- 发行版 Ubuntu 24.04 (WSL2, 内核 6.6.87.2-microsoft)，`gcc`/`make` 已有。
- **无 sudo/apt 权限**：`sudo -l` 显示 `(ALL) !ALL`，apt 完全不可用 -> 只能用户态安装到 $HOME。
- 有公网出口(github/arm/pypi 可达)，$HOME 可写、空间充足。
- **interop 关闭**：无法从 WSL 调用 Windows 的 .exe。
- WSL 网络为 **NAT 模式**（无 .wslconfig / 未设 networkingMode）。Windows 主机网关 172.24.192.1。

### 12.24.2 已完成的用户态安装（全部免 root，已验证可用）

1. **ARM GNU 工具链** 13.3.rel1：解压到 `~/toolchains/arm-gnu-toolchain-13.3.rel1-x86_64-arm-none-eabi/`。
2. **xPack OpenOCD** 0.12.0-6：解压到 `~/toolchains/xpack-openocd-0.12.0-6/`（自带 stlink.cfg / stm32g4x.cfg）。
3. **GDB 依赖修复**：Ubuntu 24.04 只有 `libncursesw.so.6`，而 arm-none-eabi-gdb 需 `.so.5`。在 `~/toolchains/compatlibs/` 建软链接 `libncursesw.so.5 -> .so.6`、`libtinfo.so.5 -> .so.6`，通过 `LD_LIBRARY_PATH` 提供。gdb 已能运行。
4. **PATH 持久化**：已写入 `~/.bashrc`（PATH + LD_LIBRARY_PATH + OPENOCD_SCRIPTS）。也可在项目里 `source tools/env-embedded.sh` 临时加载。
5. **编译验证通过**：`make CUSTOM_FD6288_G431 ARM_SDK_PREFIX=arm-none-eabi-` 成功，产物 `obj/AM32_CUSTOM_FD6288_G431_2.20.{elf,bin}`。用量 RAM 39%/FLASH 40%。
   - 注意：Makefile 用 `$(ARM_SDK_PREFIX)gcc`；因为我们把工具链放在 PATH 上，传 `ARM_SDK_PREFIX=arm-none-eabi-` 即可复用，无需让 AM32 自带的 tools.mk 去联网下载工具链。

新增文件：
- `tools/env-embedded.sh`：一键把工具链加入当前 shell 的 PATH。
- `tools/openocd-stm32g431-stlink.cfg`：G431 + ST-Link(SWD) 的 OpenOCD 配置，含 `program_fw` 烧录 proc。

### 12.24.3 USB 直通到 WSL 的结论：标准 usbipd 路线被 root 约束堵死

把 Windows 上的 ST-Link USB 转发进 WSL，标准做法是 usbipd-win + WSL 内 `usbip attach`。但本环境：
- WSL 内 **无 usbip 客户端**（apt 装不了，linux-tools 也没有）。
- `usbip attach` 需要内核 **vhci-hcd** 模块，`modprobe vhci_hcd` 报 `Operation not permitted`（需 root）。
- interop 关闭，无法从 WSL 侧驱动 Windows 的 usbipd。

=> **在当前无 root 条件下，无法把 ST-Link 直通进 WSL。** 不要在此方向继续尝试。

### 12.24.4 可行替代方案（推荐 A）

**方案 A（推荐，改动最小）：Windows 跑 OpenOCD 作为 GDB server，WSL 跑 GDB 客户端。**
- 编译仍在 WSL（已验证好用）。
- 在 Windows 上放一份 OpenOCD（xPack 的 win64 版，解压即用，**无需管理员**），连接 ST-Link，并让 GDB server 监听所有网卡：
  - Windows PowerShell 里：
    ```
    openocd.exe -f interface/stlink.cfg -c "transport select hla_swd" -f target/stm32g4x.cfg -c "bindto 0.0.0.0"
    ```
  - `bindto 0.0.0.0` 让 WSL 能通过网关 IP 访问（NAT 模式下 WSL->Windows）。
- WSL 里连接：
  ```
  source tools/env-embedded.sh
  arm-none-eabi-gdb obj/AM32_CUSTOM_FD6288_G431_2.20.elf
  (gdb) target extended-remote 172.24.192.1:3333   # Windows 网关 IP
  ```
  烧录也可直接在 Windows OpenOCD 里 `program obj/....elf verify reset`（把 elf 拷到 Windows 可见路径，或用 /mnt 路径）。
- 注意：Windows 防火墙可能拦 3333 端口，首次连接放行即可；或用 SSH 端口转发规避。

**方案 B：申请 root / 让 IT 开权限，走标准 usbipd-win + WSL usbip attach。**
- 需要：WSL 内可 `apt install usbip` + `modprobe vhci-hcd`（root），Windows 装 usbipd-win（管理员）。
- 一旦满足，直通后 WSL 内 `openocd -f tools/openocd-stm32g431-stlink.cfg` 可直接连本地 ST-Link，体验最顺。

**方案 C：全流程在 Windows 做**（放弃 WSL 工具链）。不推荐，因编译环境已在 WSL 就绪。

### 12.24.5 现状小结

- 编译器 + OpenOCD + GDB：**WSL 内已装好并验证**。
- 连接硬件：**受限于无 root，ST-Link 无法直通 WSL**；推荐用方案 A（Windows 跑 OpenOCD server，WSL 跑 GDB client）打通烧录与调试，再配合 12.22 的 dbg_buf 抓取脚本做低速 ZCD 诊断。
